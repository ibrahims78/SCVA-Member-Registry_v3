# نظام إدارة أعضاء الرابطة السورية لأمراض وجراحة القلب
## SCVA Members Management System — Desktop Edition

**الإصدار:** 1.7.0  
**التاريخ:** يونيو 2026  
**المنصة:** Windows 10 / 11 (64-bit)

---

## 📦 محتويات مجلد `releases/`

| الملف | الوصف | الحجم |
|---|---|---|
| `SCVA-Members-v1.7.0-win32-x64.zip` | أرشيف الإصدار 1.7.0 (أحدث) | ~110 MB |
| `SCVA-Diagnostics-GUIDE.txt` | دليل أداة التشخيص الشامل (عربي) — v1.7.0 | |
| `README.md` | هذا الملف | |

---

## 🚀 تثبيت التطبيق على Windows

### الخطوات:

1. **فك ضغط الأرشيف** باستخدام [7-Zip](https://www.7-zip.org/) أو WinRAR أو "استخراج الكل" من ويندوز

2. **افتح المجلد** `SCVA Members-win32-x64`

3. **شغّل التطبيق** بالنقر المزدوج على `SCVA Members.exe`

> **ملاحظة:** قد يُظهر Windows تحذيراً (SmartScreen) لأن التطبيق غير موقَّع رقمياً.  
> اضغط **"معلومات إضافية"** ثم **"تشغيل على أي حال"** للمتابعة.

---

## 🔑 أول تشغيل

عند فتح التطبيق لأول مرة:

1. **ستظهر كلمة المرور الأولية** في نافذة الطرفية (Terminal) التي تفتح مع التطبيق
2. **بيانات الدخول:**
   - اسم المستخدم: `admin`
   - كلمة المرور: تُطبع مرة واحدة في السجل عند أول تشغيل
3. سيطلب منك التطبيق **تغيير كلمة المرور** فور الدخول الأول
4. بعد التغيير — أنت جاهز!

---

## 💾 موقع البيانات

تُحفظ جميع بيانات الأعضاء في **نفس مجلد التطبيق**:

```
[مجلد التطبيق]\scva-members.db
```

مثال:
```
C:\SCVA Members_win\scva-members.db
```

### مزايا الموقع
- 📦 **نسخ احتياطي سهل** — الملف بجانب التطبيق مباشرةً
- 🚚 **محمول تماماً** — انقل المجلد كاملاً (USB أو حافظة) وستنتقل البيانات معه
- 👥 **مشترك بين جميع مستخدمي Windows** على نفس الجهاز

### النسخ الاحتياطي
- استخدم ميزة **النسخ الاحتياطي** داخل التطبيق (الإعدادات → تصدير JSON)
- أو انسخ ملف `scva-members.db` مباشرةً من مجلد التطبيق
- استعِد البيانات في أي وقت عبر ميزة **استيراد من نسخة احتياطية**

### الترقية من نسخة سابقة
انقل ملف قاعدة البيانات إلى مجلد التطبيق الجديد. إذا كانت قاعدة البيانات في AppData (إصدار قديم جداً قبل v1.5.0):
```
من: C:\Users\[اسم المستخدم]\AppData\Roaming\SCVA Members\scva-members.db
إلى: [مجلد التطبيق الجديد]\scva-members.db
```

---

## 🔧 أداة التشخيص (SCVA-Diagnostics.bat)

مضمّنة داخل مجلد التطبيق. تستخدمها عند وجود أي مشكلة:

1. انقر بزر الأيمن على `SCVA-Diagnostics.bat` ← **تشغيل كمسؤول**
2. اختر **`3`** للتشخيص الكامل (موصى به)
3. أرسل ملف `SCVA-Diagnostics-Report.txt` لفريق الدعم إذا احتجت مساعدة

للاطلاع على الشرح الكامل لكل فحص: `SCVA-Diagnostics-GUIDE.txt`

---

## ✨ المميزات

| الميزة | الحالة |
|---|---|
| إدارة الأعضاء (إضافة / تعديل / حذف) | ✅ |
| تتبع الاشتراكات والدفعات السنوية | ✅ |
| لوحة تحكم بالإحصائيات والرسوم البيانية | ✅ |
| استيراد الأعضاء من Excel | ✅ |
| استيراد الاشتراكات من Excel | ✅ |
| تصدير القوائم إلى Excel | ✅ |
| تصدير بطاقة العضو إلى Word | ✅ |
| تصدير بطاقة العضو إلى PDF (Electron native) | ✅ |
| نسخ احتياطي كامل (JSON) | ✅ |
| استيراد من نسخة احتياطية JSON | ✅ |
| مسح جميع البيانات والعودة للبداية | ✅ |
| **حفظ تلقائي كل 5 ثوانٍ** | ✅ |
| **قاعدة البيانات في مجلد التطبيق (USB-ready)** | ✅ |
| **استرداد تلقائي من ملف .tmp** | ✅ |
| **إعادة محاولة تلقائية عند فشل جلب البيانات** | ✅ |
| **إعادة تحميل شاملة لجميع البيانات بعد تسجيل الدخول** | ✅ |
| **شاشة تحميل أثناء الإقلاع** | ✅ |
| **محرك قاعدة البيانات WASM مضمون دائماً** | ✅ |
| **أداة تشخيص مدمجة** | ✅ |
| إدارة المستخدمين والصلاحيات | ✅ |
| واجهة عربية كاملة (RTL) | ✅ |
| واجهة إنجليزية | ✅ |
| الوضع الليلي والنهاري | ✅ |
| يعمل بدون إنترنت تماماً | ✅ |
| لا يحتاج تثبيت Node.js أو أي برامج إضافية | ✅ |
| قاعدة بيانات محلية (SQLite) — لا خوادم خارجية | ✅ |

---

## ⚙️ متطلبات النظام

- **نظام التشغيل:** Windows 10 أو 11 (64-bit)
- **المعالج:** أي معالج حديث (Intel / AMD)
- **الذاكرة:** 4 GB RAM كحد أدنى (8 GB مستحسن)
- **المساحة:** 500 MB فضاء خالٍ
- **إنترنت:** غير مطلوب للتشغيل

---

## 🗂️ هيكل مجلد التطبيق

```
SCVA Members-win32-x64/
├── SCVA Members.exe           ← التطبيق الرئيسي
├── scva-members.db            ← قاعدة البيانات (تُنشأ عند أول تشغيل)
├── SCVA-Diagnostics.bat       ← أداة التشخيص
├── resources/
│   ├── app.asar               ← كود التطبيق (مضغوط)
│   └── app.asar.unpacked/     ← ملفات WASM (خارج الضغط — مضمونة)
│       └── node_modules/sql.js/dist/sql-wasm.wasm
├── locales/                   ← ملفات اللغات
├── *.dll                      ← مكتبات النظام (Chromium)
└── ...
```

---

## 🐛 حل المشكلات

**التطبيق لا يفتح:**
- تأكد أن نظامك 64-bit
- جرّب تشغيل `SCVA Members.exe` كمسؤول (Run as Administrator)
- أضف مجلد التطبيق كاستثناء في مكافحة الفيروسات

**البيانات لا تظهر بعد إعادة التشغيل:**
- هذه المشكلة تم إصلاحها نهائياً في **v1.6.1** و **v1.7.0**
- تأكد أنك تستخدم الإصدار الأحدث
- إذا استمرت: شغّل `SCVA-Diagnostics.bat` للتشخيص الكامل

**لا تظهر كلمة المرور الأولية:**
- انظر إلى نافذة الطرفية (cmd) التي تفتح مع التطبيق

**بطء عند أول تشغيل:**
- طبيعي — ستظهر شاشة تحميل حتى يصبح الخادم جاهزاً

**مكافحة الفيروسات تحجب التطبيق:**
- أضف مجلد التطبيق كاستثناء:
  ```
  powershell -Command "Add-MpPreference -ExclusionPath 'C:\SCVA Members_win'"
  ```

---

## 🏗️ إعادة البناء من المصدر (للمطورين)

```cmd
cd desktop
npm install
cd ..
npm install
cd desktop
node build.js
```

الملف الناتج في: `releases\build\SCVA Members-win32-x64\`

---

## 📋 سجل الإصدارات

### الإصدار 1.7.0 (يونيو 2026)
- 🐛 إصلاح: رقم الإصدار في الواجهة كان "1.1.0" — أصبح "1.7.0" ✅
- 🔒 تحسين أمني: استبدال `Math.random()` بـ `crypto.randomBytes(32)` في سر الجلسة
- 🔧 تحديث: إصدار ملف النسخ الاحتياطي "1.1" → "1.7"
- 📄 تحديث شامل لـ `SCVA-Diagnostics-GUIDE.txt` إلى v1.7.0
- 🔢 توحيد أرقام الإصدارات في جميع الملفات (package.json × 2، brand.ts، main.js، Diagnostics.bat)

### الإصدار 1.6.1 (يونيو 2026)
- 🐛 إصلاح جذري: البيانات لا تظهر بعد إعادة التشغيل
  - السبب الجذري: `MembersProvider` يطلق `/api/members` قبل تسجيل الدخول → 401 → حالة خطأ دائمة
  - الإصلاح: بعد تسجيل الدخول يُعاد تحميل **جميع** الاستعلامات (لا `/api/user` وحده)

### الإصدار 1.6.0 (يونيو 2026)
- 🐛 إصلاح جزئي: `retry: false` → `retry: 3` مع تأخير تصاعدي

### الإصدار 1.5.0 (يونيو 2026)
- 🔧 تغيير: قاعدة البيانات تُحفظ الآن في مجلد التطبيق بدلاً من AppData
- 🐛 إصلاح: استرداد تلقائي من ملف `.tmp` عند بدء التشغيل
- ✨ تحسين: شاشة تحميل جميلة أثناء الإقلاع + انتظار 30 ثانية
- 🔧 أداة التشخيص v1.5.0 محدّثة لمسار البيانات الجديد

### الإصدار 1.4.0 (يونيو 2026)
- 🐛 إصلاح: `app.setName('SCVA Members')` لضمان مسار userData الصحيح دائماً
- 🔧 إضافة: أداة التشخيص `SCVA-Diagnostics.bat` v1.4.0 مدمجة

### الإصدار 1.3.0 (يونيو 2026)
- 🐛 إصلاح: ملف WASM مفقود → نسخ يدوي مضمون بعد التحزيم

### الإصدار 1.2.0 (يونيو 2026)
- 🐛 إصلاح اختفاء البيانات (EPERM في renameSync على Windows)
- ⚡ تقليل فترة الحفظ التلقائي من 15 ثانية إلى 5 ثوانٍ

### الإصدار 1.1.0 (يونيو 2026)
- ✅ استيراد من نسخة احتياطية JSON
- ✅ مسح جميع البيانات

### الإصدار 1.0.0 (يونيو 2024)
- إصدار أولي

---

```
الإصدار الحالي: 1.7.0
تاريخ البناء:   يونيو 2026
المحرك:         Electron 28 + Node.js 18
الواجهة:        React 19 + Tailwind CSS + shadcn/ui
قاعدة البيانات: SQLite (عبر sql.js — لا مكتبات مُجمَّعة)
المنصة المدعومة: Windows 10/11 x64
```

---

*الرابطة السورية لأمراض وجراحة القلب — SCVA*  
*Syrian Cardiovascular Association*
