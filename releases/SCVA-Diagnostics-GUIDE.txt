
================================================================
  دليل أداة التشخيص الشامل — SCVA-Diagnostics.bat
  نظام إدارة أعضاء الرابطة السورية لأمراض وجراحة القلب
================================================================
  الإصدار      : 1.2.0
  الهدف        : شرح كامل وتفصيلي لملف SCVA-Diagnostics.bat
  المنصة       : Windows 10 / 11 (64-bit)
================================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الأول — ما هي هذه الأداة وما الهدف منها؟
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SCVA-Diagnostics.bat هو ملف تشخيص شامل مكتوب بلغة Windows Batch
Script. الهدف منه اكتشاف أي مشكلة تمنع تطبيق "SCVA Members" من
العمل بشكل صحيح — وتحديداً مشاكل قاعدة البيانات وفقدان البيانات
عند إعادة التشغيل.

تكمن أهميته في:

  ✦ يعمل بدون تثبيت أي برنامج إضافي
  ✦ لا يُعدّل أي ملف أو إعداد (يقرأ فقط، لا يكتب إلا ملف تقرير)
  ✦ يُنتج تقرير نصي كاملاً يمكن إرساله لفريق الدعم
  ✦ يعطي نتائج واضحة: [PASS] [WARN] [FAIL]
  ✦ يُقدّم الحلول فوراً عند اكتشاف أي مشكلة


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الثاني — طريقة الاستخدام الصحيحة
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

خطوة 1: الموقع الصحيح للملف
──────────────────────────────
ضع ملف SCVA-Diagnostics.bat داخل مجلد التطبيق مباشرة:

  ✦ الصحيح:   C:\SCVA Members_win\SCVA-Diagnostics.bat
  ✗ الخطأ:    C:\SCVA-Diagnostics.bat   ← مجلد مختلف لا يعمل

السبب: الملف يحدد مجلد التطبيق تلقائياً من موقعه الخاص (%~dp0)،
فإذا كان في مجلد مختلف لن يجد SCVA Members.exe ولا ملفات resources.


خطوة 2: تشغيل الملف
──────────────────────
انقر بزر الماوس الأيمن على الملف ← "تشغيل كمسؤول" (Run as administrator)

لماذا كمسؤول؟ لأن بعض الفحوصات تحتاج صلاحيات مرتفعة:
  - قراءة إعدادات Windows Defender
  - فحص قائمة العمليات النشطة
  - فحص المنافذ الشبكية (netstat)
  - اختبار الكتابة وإعادة التسمية


خطوة 3: اختيار وضع التشخيص
──────────────────────────────
عند التشغيل يطلب الملف اختيار أحد ثلاثة أوضاع:

  ┌─────────────────────────────────────────────────────────┐
  │  [1] فحوصات ما قبل التشغيل (Pre-launch)                │
  │      شغّله قبل فتح تطبيق SCVA                          │
  │      يفحص: النظام، ملفات التطبيق، قاعدة البيانات،      │
  │      الصلاحيات، Windows Defender                        │
  │                                                         │
  │  [2] فحوصات ما بعد التشغيل (Post-launch)               │
  │      شغّله بعد فتح تطبيق SCVA وتسجيل الدخول            │
  │      يفحص: العمليات، الخادم المحلي، API، استمرارية     │
  │      البيانات عند الإغلاق                               │
  │                                                         │
  │  [3] التشخيص الكامل ← الموصى به دائماً                 │
  │      يجمع الوضعين 1 و 2 معاً بالتسلسل                  │
  └─────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الثالث — المتغيرات والمسارات التي يعمل بها الملف
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

يُعرّف الملف عند بدئه هذه المتغيرات تلقائياً:

  APP_DIR   = المجلد الذي يوجد فيه هذا الملف .bat
              مثال: C:\SCVA Members_win

  APP_EXE   = مسار ملف التطبيق التنفيذي
              مثال: C:\SCVA Members_win\SCVA Members.exe

  DATA_DIR  = مجلد بيانات المستخدم (يحدده Electron تلقائياً)
              مثال: C:\Users\admin\AppData\Roaming\SCVA Members

  DB_FILE   = ملف قاعدة البيانات SQLite
              مثال: C:\Users\admin\AppData\Roaming\SCVA Members\scva-members.db

  DB_TMP    = ملف قاعدة البيانات المؤقت (يُستخدم أثناء الحفظ)
              مثال: C:\Users\admin\AppData\Roaming\SCVA Members\scva-members.db.tmp

  WASM      = ملف محرك قاعدة البيانات (WebAssembly)
              مثال: C:\SCVA Members_win\resources\app.asar.unpacked\
                    node_modules\sql.js\dist\sql-wasm.wasm

  APP_PORT  = منفذ الخادم المحلي: 43210 (ثابت)

  REPORT    = ملف التقرير الناتج
              مثال: C:\SCVA Members_win\SCVA-Diagnostics-Report.txt

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الرابع — شرح كل مرحلة تفصيلياً
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


════════════════════════════════════════════════════════════════
  المرحلة 1 — نظام التشغيل والمعدات (Operating System & Hardware)
════════════════════════════════════════════════════════════════

هذه المرحلة تتحقق من أن البيئة تستوفي الحد الأدنى لتشغيل التطبيق.

┌─ الفحص 1: نسخة Windows (Windows Version) ──────────────────┐
│                                                              │
│  الأمر المستخدم: ver                                        │
│  المتوقع: رقم إصدار رئيسي 10 أو أعلى (Win10/Win11)         │
│                                                              │
│  [PASS] major version 10 ← Windows 10 أو 11                │
│  [FAIL] major version < 10 ← نظام قديم غير مدعوم           │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: معمارية النظام (Architecture) ───────────────────┐
│                                                              │
│  يقرأ متغير: PROCESSOR_ARCHITECTURE و PROCESSOR_ARCHITEW6432│
│  المتوقع: AMD64 (أي 64-bit)                                 │
│                                                              │
│  [PASS] AMD64 ← 64-bit مؤكد                                │
│  [PASS] WOW64 ← برنامج 32-bit يعمل على نظام 64-bit         │
│  [FAIL] غير ذلك ← نظام 32-bit لا يدعمه التطبيق            │
│                                                              │
│  لماذا مهم؟ تطبيق SCVA مُبنى لـ win32-x64 فقط.            │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: صلاحيات المسؤول (Admin Rights) ─────────────────┐
│                                                              │
│  الأمر المستخدم: net session                                │
│  يُعيد errorlevel=0 فقط عند تشغيل التطبيق كمسؤول          │
│                                                              │
│  [PASS] errorlevel=0 ← يعمل كمسؤول                        │
│  [WARN] errorlevel≠0 ← ليس مسؤولاً، بعض الفحوصات قد تفشل │
│                                                              │
│  ملاحظة: هذا تحذير وليس خطأ — التطبيق نفسه لا يشترط       │
│  صلاحيات المسؤول للتشغيل، لكن أداة التشخيص قد تُعطي        │
│  نتائج غير دقيقة بدونها.                                   │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 4: الذاكرة العشوائية (RAM) ────────────────────────┐
│                                                              │
│  الأمر المستخدم: PowerShell                                 │
│    [Math]::Round((Get-CimInstance Win32_OperatingSystem)     │
│    .TotalVisibleMemorySize/1024)                             │
│  النتيجة بالميغابايت تُكتب في ملف مؤقت ثم تُقرأ.           │
│                                                              │
│  [PASS] >= 4096 MB ← يفوق الحد الأدنى                     │
│  [WARN] < 4096 MB و > 0 ← أقل من المستحسن                 │
│  [WARN] = 0 ← فشل في قراءة الذاكرة                        │
│                                                              │
│  لماذا ملف مؤقت؟ طريقة backtick في Batch غير موثوقة        │
│  على بعض إصدارات Windows، لذا يكتب PowerShell النتيجة     │
│  في %TEMP%\scva_mem.tmp ثم يقرأها Batch منه.               │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 5: مساحة القرص (Disk Space) ───────────────────────┐
│                                                              │
│  الأمر المستخدم: PowerShell                                 │
│    [Math]::Round((Get-PSDrive C).Free/1MB)                  │
│  يقيس المساحة الخالية على محرك C: بالميغابايت.             │
│                                                              │
│  [PASS] >= 500 MB ← كافٍ                                   │
│  [FAIL] < 500 MB و > 0 ← غير كافٍ، قد لا يُنشئ التطبيق   │
│          ملف قاعدة البيانات                                 │
│  [WARN] = 0 ← فشل في القراءة                              │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 2 — ملفات التطبيق (Application Files)
════════════════════════════════════════════════════════════════

┌─ الفحص 1: الملف التنفيذي (App EXE) ───────────────────────┐
│                                                              │
│  يبحث عن: SCVA Members.exe في APP_DIR                      │
│                                                              │
│  [PASS] موجود ← يعرض حجمه بالبايت                         │
│  [FAIL] غير موجود ← يعني أن الملف .bat ليس في مجلد       │
│          التطبيق الصحيح، أو التطبيق غير مُثبَّت.           │
│                                                              │
│  إذا ظهر [FAIL] هنا: انقل SCVA-Diagnostics.bat            │
│  إلى مجلد التطبيق وأعد تشغيله.                            │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: موارد التطبيق (App Resources) ──────────────────┐
│                                                              │
│  يبحث عن: resources\app.asar                               │
│  هذا الأرشيف هو الكود الكامل للتطبيق (Electron + React)   │
│                                                              │
│  [PASS] موجود ← التطبيق مُثبَّت بشكل صحيح                 │
│  [WARN] غير موجود ← قد يكون الأرشيف غير مُستخرج بالكامل  │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: ملف WASM — محرك قاعدة البيانات ─────────────────┐
│                                                              │
│  هذا أهم فحص في المرحلة الثانية بالكامل.                   │
│                                                              │
│  يبحث عن:                                                   │
│    resources\app.asar.unpacked\node_modules\                 │
│    sql.js\dist\sql-wasm.wasm                                 │
│                                                              │
│  ما هو هذا الملف؟                                           │
│  sql-wasm.wasm هو ملف WebAssembly يحتوي على محرك SQLite     │
│  المُترجَم. التطبيق يُحمّله في الذاكرة ليتمكن من فتح       │
│  قاعدة البيانات وقراءتها وكتابتها. بدونه لا تعمل           │
│  قاعدة البيانات إطلاقاً.                                    │
│                                                              │
│  لماذا يجب أن يكون خارج app.asar؟                           │
│  ملفات WebAssembly لا يمكن تنفيذها من داخل أرشيف .asar    │
│  (وهو نظام ملفات افتراضي). يجب أن تكون على القرص الفعلي   │
│  ليتمكن النظام من تحميلها. لهذا يوجد مجلد                 │
│  app.asar.unpacked/ الذي يحتوي الملفات المُستخرَجة.         │
│                                                              │
│  [PASS] موجود ← يعرض حجمه (~644 KB)                        │
│  [FAIL] غير موجود ← كارثة! سلسلة الفشل:                   │
│          WASM مفقود                                          │
│              ↓                                               │
│          محرك قاعدة البيانات يفشل في التهيؤ                │
│              ↓                                               │
│          لا تُفتح أي قاعدة بيانات                          │
│              ↓                                               │
│          جميع API تُعيد أخطاء                              │
│              ↓                                               │
│          لا يُكتب أي ملف .db على القرص                    │
│              ↓                                               │
│          كل البيانات تضيع عند الإغلاق                      │
│                                                              │
│  هذه بالضبط هي المشكلة التي ظهرت في التقرير التشخيصي      │
│  وتم إصلاحها في الإصدار 1.3.0.                             │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 3 — مجلد البيانات وملف قاعدة البيانات
════════════════════════════════════════════════════════════════

┌─ الفحص 1: مجلد البيانات (Data Directory) ─────────────────┐
│                                                              │
│  يبحث عن: %APPDATA%\SCVA Members\                          │
│  ما هو؟ Electron يُنشئ هذا المجلد تلقائياً عند أول تشغيل  │
│  للتطبيق، ويستخدمه لحفظ قاعدة البيانات والإعدادات.        │
│                                                              │
│  [PASS] موجود                                               │
│  [WARN] غير موجود ← طبيعي عند أول تشغيل، سيُنشأ           │
│          تلقائياً عند فتح التطبيق                          │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: الملف المؤقت العالق (Stuck Temp File) ──────────┐
│                                                              │
│  يبحث عن: scva-members.db.tmp                              │
│                                                              │
│  ما معناه وجود هذا الملف؟                                   │
│  عند كل حفظ، التطبيق يتبع هذه الخطوات:                    │
│    1. يكتب البيانات في scva-members.db.tmp (ملف مؤقت)     │
│    2. يُعيد تسمية .tmp إلى .db (الكتابة الذرية)            │
│                                                              │
│  وجود .tmp يعني أن الخطوة 2 (إعادة التسمية) كانت تفشل     │
│  دائماً ← الملف الأصلي .db لا يُحدَّث أبداً ← فقدان       │
│  البيانات. هذا هو السبب الجذري لمشكلة الإصدار 1.1.0.       │
│                                                              │
│  [PASS] غير موجود ← لا توجد آثار للمشكلة القديمة          │
│  [FAIL] موجود ← دليل قاطع أن المشكلة وُجدت على هذا الجهاز │
│          الحل: del "%APPDATA%\SCVA Members\scva-members.db.tmp"│
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: ملف قاعدة البيانات (Database File) ─────────────┐
│                                                              │
│  يبحث عن: scva-members.db ويتحقق من حجمه وتاريخ تعديله   │
│                                                              │
│  [PASS] موجود وحجمه > 0 ← يعرض الحجم وتاريخ آخر تعديل   │
│  [FAIL] حجمه 0 بايت ← قاعدة البيانات فارغة أو تالفة       │
│  [WARN] غير موجود ← طبيعي في أول تشغيل                    │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 4 — صلاحيات الكتابة واختبار إعادة التسمية
════════════════════════════════════════════════════════════════

هذه أهم مرحلة لتشخيص مشكلة فقدان البيانات.

┌─ الفحص 1: إنشاء مجلد البيانات (Create Data Dir) ──────────┐
│                                                              │
│  إذا لم يكن مجلد البيانات موجوداً، يحاول إنشاءه.          │
│                                                              │
│  [PASS] نجح الإنشاء                                         │
│  [FAIL] فشل الإنشاء ← مشكلة صلاحيات حرجة                 │
│          يُوقف باقي فحوصات هذه المرحلة (SKIP_WRITE_TESTS)  │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: صلاحية الكتابة (Write Access) ──────────────────┐
│                                                              │
│  ينشئ ملف مؤقت: diag-write.tmp في مجلد البيانات           │
│  ثم يحذفه فوراً. هذا يُثبت أن العملية يمكنها الكتابة.     │
│                                                              │
│  [PASS] كتب وحذف بنجاح ← صلاحيات الكتابة مؤكدة           │
│  [FAIL] لم يُنشأ الملف ← مجلد البيانات محمي ضد الكتابة   │
│          يُوقف باقي الفحوصات                               │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: اختبار إعادة التسمية الذرية (Atomic Rename) ────┐
│                                                              │
│  هذا الفحص يُحاكي بالضبط ما يفعله التطبيق عند الحفظ:      │
│    1. ينشئ ملف: diag-test.tmp                              │
│    2. يُحاول إعادة تسميته إلى: diag-test.db                │
│    3. يتحقق من وجود diag-test.db                           │
│    4. يحذف ملفات الاختبار                                  │
│                                                              │
│  [PASS] نجحت إعادة التسمية ← آلية الحفظ الذرية تعمل       │
│  [FAIL] فشلت إعادة التسمية (EPERM) ←                       │
│          هذا هو السبب الجذري لفقدان البيانات في v1.1.0     │
│          الإصدار 1.2.0 يتجاوز هذا بالكتابة المباشرة        │
│                                                              │
│  لماذا تفشل إعادة التسمية على Windows؟                     │
│  غالباً بسبب:                                               │
│    - Windows Defender يفحص الملف أثناء إعادة التسمية      │
│    - مضاد فيروسات آخر يحجز مؤشراً (handle) على الملف      │
│    - برنامج نسخ احتياطي يقرأ الملف في نفس اللحظة          │
│    - صلاحيات مقيدة بسياسة المجموعة (Group Policy)          │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 4: قفل ملف قاعدة البيانات (DB File Lock) ──────────┐
│                                                              │
│  إذا كان ملف .db موجوداً، يحاول فتحه بوضع ReadWrite        │
│  باستخدام PowerShell و [IO.File]::Open(...)                │
│                                                              │
│  [PASS] UNLOCKED ← الملف غير مُقفَل، يمكن الكتابة إليه   │
│  [FAIL] LOCKED ← برنامج آخر يحجز الملف (ربما نسخة         │
│          ثانية من SCVA أو مضاد الفيروسات)                  │
│          الحل: أغلق جميع نوافذ SCVA وأعد المحاولة         │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 5 — Windows Defender ومضاد الفيروسات
════════════════════════════════════════════════════════════════

┌─ الفحص 1: استثناء مجلد البيانات (Defender - Data Dir) ────┐
│                                                              │
│  يتحقق هل مجلد %APPDATA%\SCVA Members مُستثنى              │
│  من فحص Windows Defender الفوري (Real-time Protection).    │
│                                                              │
│  الأمر المستخدم:                                            │
│    Get-MpPreference | Select ExclusionPath                  │
│  يبحث عن أي مسار يحتوي "SCVA" في قائمة الاستثناءات.       │
│                                                              │
│  [PASS] EXCLUDED ← مُستثنى، لن يتدخل Defender              │
│  [WARN] NOT_EXCLUDED ← قد يتسبب في خطأ EPERM عند الحفظ   │
│          الحل:                                              │
│          powershell -Command "Add-MpPreference              │
│            -ExclusionPath '%APPDATA%\SCVA Members'"         │
│  [WARN] UNAVAILABLE ← لا يمكن قراءة إعدادات Defender      │
│          (قد يكون Defender معطلاً أو مُدار بـ Group Policy) │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: استثناء مجلد التطبيق (Defender - App Dir) ──────┐
│                                                              │
│  نفس المنطق لكن لمجلد التطبيق نفسه (C:\SCVA Members_win)  │
│                                                              │
│  [PASS] EXCLUDED ← مثالي                                   │
│  [WARN] NOT_EXCLUDED ← قد يُبطّئ التشغيل لأن Defender      │
│          يفحص ملفات الـ .asar عند كل تشغيل                │
│          الحل:                                              │
│          powershell -Command "Add-MpPreference              │
│            -ExclusionPath 'C:\SCVA Members_win'"            │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: الملفات المُعزَلة (Defender - Threats) ──────────┐
│                                                              │
│  يبحث في سجل Defender عن أي ملف مُعزَل (quarantined)      │
│  يحتوي اسمه "SCVA".                                        │
│                                                              │
│  الأمر:                                                     │
│    Get-MpThreatDetection | Where-Object {                   │
│      $_.Resources -match 'SCVA' }                           │
│                                                              │
│  [PASS] NONE ← لا توجد ملفات SCVA مُعزَلة                 │
│  [FAIL] THREAT: ... ← Defender عزَل ملفاً من التطبيق!     │
│          يجب استعادته من عزل Defender:                     │
│          Windows Security → Protection History → Restore    │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 4: المنفذ 43210 (Port 43210) ──────────────────────┐
│                                                              │
│  التطبيق يُشغّل خادم Express على المنفذ 43210 محلياً.      │
│  يتحقق هل هذا المنفذ مُستخدَم قبل تشغيل التطبيق.          │
│                                                              │
│  الأمر: Get-NetTCPConnection -LocalPort 43210               │
│                                                              │
│  [PASS] FREE ← المنفذ حر، التطبيق سيعمل بدون مشكلة        │
│  [WARN] IN_USE ← شيء ما يستخدم المنفذ بالفعل              │
│          غالباً يعني التطبيق مفتوح بالفعل من نافذة أخرى   │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 6 — العملية والخادم المحلي (وضع ما بعد التشغيل)
════════════════════════════════════════════════════════════════

  ← هذه الفحوصات تعمل فقط بعد تشغيل التطبيق

┌─ الفحص 1: عملية التطبيق (App Process) ────────────────────┐
│                                                              │
│  الأمر: tasklist | findstr /i "SCVA Members.exe"            │
│                                                              │
│  [PASS] موجود ← التطبيق يعمل                               │
│  [FAIL] غير موجود ← التطبيق غير مشغَّل أو توقف بسبب خطأ  │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: الخادم المحلي (Server Listening) ────────────────┐
│                                                              │
│  الأمر: netstat -ano | findstr "LISTENING" | findstr ":43210"│
│  يبحث عن خادم Express يستمع على المنفذ 43210               │
│                                                              │
│  [PASS] ← يعرض رقم العملية (PID)                           │
│  [FAIL] ← الخادم لم يبدأ                                   │
│           السبب الأرجح: فشل تحميل WASM ← قاعدة البيانات    │
│           لم تُهيَّأ ← الخادم يتوقف بالكامل               │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 3: استجابة HTTP (HTTP Response) ────────────────────┐
│                                                              │
│  يرسل طلب GET إلى: http://127.0.0.1:43210/api/user          │
│  باستخدام PowerShell Invoke-WebRequest                      │
│                                                              │
│  [PASS] HTTP_401 ← الخادم يعمل، المستخدم غير مسجَّل دخوله │
│           هذه النتيجة الطبيعية قبل تسجيل الدخول            │
│  [PASS] HTTP_200 ← الخادم يعمل والجلسة نشطة               │
│  [FAIL] UNREACHABLE ← الخادم لا يستجيب على المنفذ 43210   │
│  [WARN] أي استجابة أخرى                                    │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 7 — قاعدة البيانات أثناء تشغيل التطبيق
════════════════════════════════════════════════════════════════

┌─ الفحص 1: حجم قاعدة البيانات أثناء التشغيل (DB Size Live) ┐
│                                                              │
│  يقرأ حجم scva-members.db أثناء تشغيل التطبيق              │
│  ويعرض: الحجم بالبايت وتاريخ آخر تعديل                    │
│                                                              │
│  [PASS] > 20480 بايت (20KB) ← قاعدة بيانات تحتوي بيانات  │
│  [WARN] 4096-20480 بايت ← صغيرة، ربما لا يوجد أعضاء بعد  │
│  [WARN] 1-4096 بايت ← صغيرة جداً، ربما هيكل فارغ فقط     │
│  [FAIL] 0 بايت ← لا شيء يُكتب على القرص                  │
│  [FAIL] غير موجودة ← أسوأ سيناريو، الخادم لا يحفظ أي شيء │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: الملف المؤقت أثناء التشغيل (Temp File Live) ────┐
│                                                              │
│  يبحث عن scva-members.db.tmp أثناء تشغيل التطبيق           │
│                                                              │
│  [PASS] غير موجود ← الحفظ يكتمل بنجاح في كل مرة          │
│  [FAIL] موجود ← إعادة التسمية تفشل الآن وفي كل حفظ        │
│           (هذا دليل فوري على مشكلة EPERM النشطة)           │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 8 — اختبار نقاط API
════════════════════════════════════════════════════════════════

يختبر نقطتي API رئيسيتين للتحقق من أن الخادم يستجيب:

┌─ GET /api/members ──────────────────────────────────────────┐
│                                                              │
│  يجلب قائمة الأعضاء.                                        │
│                                                              │
│  [PASS] HTTP 200 ← الخادم يعمل والجلسة نشطة              │
│  [WARN] HTTP 401 ← يحتاج تسجيل دخول أولاً                 │
│  [FAIL] UNREACHABLE أو أي خطأ ← الخادم متوقف               │
└──────────────────────────────────────────────────────────────┘

┌─ GET /api/stats ────────────────────────────────────────────┐
│                                                              │
│  يجلب إحصائيات لوحة التحكم (عدد الأعضاء، الاشتراكات...).  │
│                                                              │
│  [PASS] HTTP 200                                            │
│  [WARN] HTTP 401 أو أي استجابة أخرى                        │
└──────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════
  المرحلة 9 — اختبار الاستمرارية بعد إعادة التشغيل
════════════════════════════════════════════════════════════════

هذا الاختبار هو جوهر تشخيص مشكلة فقدان البيانات.

التسلسل الكامل:
  1. أضف عضواً في التطبيق
  2. أغلق التطبيق (اضغط X)
  3. انتظر 3-5 ثوانٍ
  4. اضغط ENTER في نافذة الأداة

┌─ الفحص 1: هل حُفظت البيانات؟ (Data Saved) ────────────────┐
│                                                              │
│  بعد إغلاق التطبيق، يفحص حجم scva-members.db              │
│                                                              │
│  [PASS] > 20480 بايت ← البيانات محفوظة على القرص بشكل     │
│           صحيح، المشكلة غير موجودة                         │
│  [WARN] 4096-20480 بايت ← هل أضفت عضواً فعلاً؟           │
│  [FAIL] ملف غير موجود ← أسوأ نتيجة ممكنة: كل البيانات    │
│           ضاعت، التطبيق لا يكتب على القرص إطلاقاً.         │
│           السبب المؤكد: ملف WASM مفقود (المرحلة 2)         │
└──────────────────────────────────────────────────────────────┘

┌─ الفحص 2: الحفظ النهائي عند الإغلاق (Final Quit Save) ────┐
│                                                              │
│  يبحث عن ملف .tmp بعد إغلاق التطبيق                       │
│  التطبيق يُجري حفظاً نهائياً في حدث before-quit            │
│                                                              │
│  [PASS] لا يوجد .tmp ← الحفظ النهائي اكتمل بنجاح          │
│  [FAIL] .tmp موجود بعد الإغلاق ← الحفظ النهائي فشل        │
│           ويعني أن بيانات آخر جلسة قد ضاعت                 │
└──────────────────────────────────────────────────────────────┘

خيار تخطي الاختبار:
  اكتب حرف S ثم ENTER لتخطي هذا الاختبار (SKIP)
  في هذه الحالة تُسجَّل نتيجة [WARN] "Skipped by user"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الخامس — فهم النتائج والتقرير النهائي
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

رموز النتائج:

  [PASS] = اجتاز الفحص بنجاح — كل شيء على ما يرام
  [WARN] = تحذير — ليس حرجاً لكن يستحق الانتباه
  [FAIL] = فشل حرج — يسبب مشكلة يجب حلها

عداد النتائج:
  PASS=N   WARN=N   FAIL=N   (of N checks)

التقييم النهائي (Verdict):
  ┌──────────────────────────────────────────────────────────┐
  │ ALL CHECKS PASSED  ← لا توجد مشاكل، النظام جاهز        │
  │ WARNINGS FOUND     ← توجد تحذيرات، يُستحسن مراجعتها   │
  │ ACTION REQUIRED    ← توجد أخطاء حرجة يجب حلها         │
  └──────────────────────────────────────────────────────────┘

الإجراءات المقترحة عند وجود أخطاء (ACTION REQUIRED):
  تظهر هذه الأوامر في النهاية تلقائياً وتُكتب في التقرير:

  [1] إضافة استثناء Defender لمجلد البيانات:
      powershell -Command "Add-MpPreference
        -ExclusionPath '%APPDATA%\SCVA Members'"

  [2] إضافة استثناء Defender لمجلد التطبيق:
      powershell -Command "Add-MpPreference
        -ExclusionPath 'C:\SCVA Members_win'"

  [3] إصلاح صلاحيات الكتابة:
      icacls "%APPDATA%\SCVA Members"
        /grant "%USERNAME%:(OI)(CI)F"

  [4] حذف الملف المؤقت العالق:
      del "%APPDATA%\SCVA Members\scva-members.db.tmp"

  [5] التأكد من استخدام الإصدار 1.3.0 أو أحدث


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل السادس — ملف التقرير (SCVA-Diagnostics-Report.txt)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

الملف يُنشئ تلقائياً:
  SCVA-Diagnostics-Report.txt
في نفس مجلد التطبيق.

يحتوي التقرير على:
  ✦ معلومات الجهاز (اسم الجهاز، المستخدم، التاريخ والوقت)
  ✦ المسارات المستخدمة (APP_DIR, DATA_DIR, DB_FILE, WASM)
  ✦ نتيجة كل فحص بالتفصيل
  ✦ النقاط والمسارات المقترحة للإصلاح
  ✦ أوامر الإصلاح الجاهزة للتشغيل

متى ترسل هذا الملف؟
  أرسله لفريق الدعم عند الإبلاغ عن أي مشكلة في التطبيق.
  يحتوي على كل المعلومات اللازمة لتشخيص المشكلة عن بُعد.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل السابع — الروتينات المساعدة الداخلية (Subroutines)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

الملف يستخدم أربع دوال داخلية (labels تُستدعى بـ call):

  :ok      ← يُسجَّل PASS ويزيد عداد PASS وTOTAL
             يطبع: [PASS] الاسم -- الرسالة
             يكتب نفس الشيء في ملف التقرير

  :warn    ← يُسجَّل WARN ويزيد عداد WARN وTOTAL
             يطبع: [WARN] الاسم -- الرسالة

  :bad     ← يُسجَّل FAIL ويزيد عداد FAIL وTOTAL
             يطبع: [FAIL] الاسم -- الرسالة

  :log_info ← يطبع معلومة بدون إضافة للعداد
              يُستخدم لعرض نسخة Windows فقط

طريقة الاستدعاء:
  call :ok    "اسم الفحص" "رسالة النتيجة"
  call :warn  "اسم الفحص" "رسالة التحذير"
  call :bad   "اسم الفحص" "رسالة الخطأ"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل الثامن — تفسير التقرير التشخيصي للحالة الموثَّقة
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

فيما يلي تفسير كامل لنتيجة الفحص الذي أُجري على الجهاز:

  Computer : DESKTOP-K81PJ9H
  User     : admin
  App Dir  : C:\SCVA Members_win
  Mode     : [1] Pre-launch فقط

النتائج:

  ✅ [PASS] Architecture ← النظام 64-bit AMD64، متوافق
  ✅ [PASS] Windows Version ← Windows 10 Build 19045، مدعوم
  ✅ [PASS] Admin Rights ← يعمل كمسؤول، فحوصات دقيقة
  ✅ [PASS] RAM ← 32659 MB (32 GB)، ممتاز
  ✅ [PASS] Disk Space ← 55724 MB (54 GB) خالٍ، كافٍ
  ✅ [PASS] App EXE ← SCVA Members.exe موجود (169 MB)
  ✅ [PASS] App Resources ← resources\app.asar موجود

  ❌ [FAIL] WASM Binary ← sql-wasm.wasm مفقود!
     المسار المتوقع:
     C:\SCVA Members_win\resources\app.asar.unpacked\
     node_modules\sql.js\dist\sql-wasm.wasm

     ← هذا هو الخطأ الوحيد الذي تسبب في كل المشاكل:
       ← API لا تستجيب (الخادم لم يبدأ)
       ← لا يوجد ملف scva-members.db
       ← كل البيانات المُدخَلة تضيع عند الإغلاق

  ❌ [FAIL] GET /api/members ← الخادم لم يبدأ بسبب WASM
  ⚠️ [WARN] GET /api/stats ← UNREACHABLE (نفس السبب)

  ❌ [FAIL] Data Saved ← لا يوجد ملف .db بعد الإغلاق
  ✅ [PASS] Final Quit Save ← لا .tmp بعد الإغلاق
     (التطبيق أكمل طريقة before-quit لكن لا DB لحفظها)

  Post-Launch Score: PASS=8  WARN=1  FAIL=3
  Result: ACTION REQUIRED

الحل:
  تثبيت الإصدار 1.3.0 الذي يضمن وجود sql-wasm.wasm
  في المسار الصحيح في كل بناء.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل التاسع — أسئلة شائعة
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

س: هل الأداة تُعدّل أي ملف؟
ج: لا. الأداة للقراءة فقط باستثناء:
   - ملفات مؤقتة تُنشأ وتُحذف فوراً (diag-write.tmp, diag-test.tmp)
   - ملف التقرير SCVA-Diagnostics-Report.txt

س: هل يمكن تشغيلها أكثر من مرة؟
ج: نعم، في أي وقت وعدد لا محدود من المرات.

س: لماذا تستخدم PowerShell داخل ملف .bat؟
ج: لأن Batch Script لا يملك أوامر مباشرة للوصول إلى:
   - WMI (معلومات الذاكرة والمعالج)
   - Windows Defender API
   - اختبار قفل الملفات
   PowerShell يوفر هذه القدرات ويعمل على كل Windows 10/11.

س: لماذا يُكتب PowerShell الناتج في ملف مؤقت بدلاً من قراءته مباشرة؟
ج: لأن طريقة "backtick" في Batch (`command`) غير موثوقة
   على بعض إصدارات Windows ومع بعض إعدادات الـ Locale.
   الطريقة الموثوقة: كتابة النتيجة في ملف ← قراءتها بـ set /p.

س: ماذا يعني UNREACHABLE في فحص API؟
ج: الخادم المحلي على المنفذ 43210 لا يستجيب إطلاقاً.
   الأسباب الأكثر شيوعاً:
   - التطبيق غير مفتوح
   - الخادم فشل في البدء بسبب WASM مفقود
   - جدار ناري يحجب المنفذ

س: ماذا يعني HTTP_401؟
ج: الخادم يعمل بشكل مثالي لكن الجلسة غير مفتوحة.
   هذا سلوك طبيعي وصحيح قبل تسجيل الدخول.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  الفصل العاشر — ملخص سريع لجميع الفحوصات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

 #   الفحص                    المرحلة      الأمر / الطريقة
─────────────────────────────────────────────────────────────
 1   Windows Version           OS           ver
 2   Architecture              OS           PROCESSOR_ARCHITECTURE
 3   Admin Rights              OS           net session
 4   RAM                       OS           PowerShell WMI
 5   Disk Space                OS           PowerShell Get-PSDrive
 6   App EXE                   ملفات        if exist + %%~zf
 7   App Resources             ملفات        if exist app.asar
 8   WASM Binary               ملفات        if exist sql-wasm.wasm
 9   Data Directory            DB           if exist DATA_DIR
10   Stuck Temp File           DB           if exist .db.tmp
11   Database File             DB           if exist .db + %%~zf
12   Create Data Dir           كتابة        mkdir
13   Write Access              كتابة        echo > file
14   Atomic Rename             كتابة        move /Y .tmp .db
15   DB File Lock              كتابة        PowerShell IO.File::Open
16   Defender - Data Dir       Defender     Get-MpPreference
17   Defender - App Dir        Defender     Get-MpPreference
18   Defender - Threats        Defender     Get-MpThreatDetection
19   Port 43210                Defender     Get-NetTCPConnection
20   App Process               تشغيل        tasklist + findstr
21   Server Listening          تشغيل        netstat -ano
22   HTTP Response             تشغيل        PowerShell Invoke-WebRequest
23   DB Size Live              DB حي        if exist .db + %%~zf
24   Temp File Live            DB حي        if exist .db.tmp
25   GET /api/members          API          PowerShell Invoke-WebRequest
26   GET /api/stats            API          PowerShell Invoke-WebRequest
27   Data Saved                استمرارية    if exist .db + %%~zf
28   Final Quit Save           استمرارية    if exist .db.tmp


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  نهاية الدليل
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  الرابطة السورية لأمراض وجراحة القلب — SCVA
  Syrian Cardiovascular Association
  الإصدار الحالي للتطبيق: 1.3.0

================================================================
