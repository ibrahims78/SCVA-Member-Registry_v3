# خطة تحسين الهوية البصرية وتجربة المستخدم
## نظام إدارة أعضاء الجمعية السورية لجراحة وأمراض القلب والشرايين (SCVA)

> **الإصدار:** 1.0  
> **التاريخ:** 28 نيسان 2026  
> **المؤلف:** مراجعة احترافية لواجهة وتجربة المستخدم  
> **الجمهور:** فريق التطوير، مدير المنتج، صاحب القرار في الجمعية

---

## 1. ملخص تنفيذي

التطبيق يعمل اليوم بشكل وظيفي وآمن بعد جولة الإصلاحات الأمنية الأخيرة، لكنّ البصمة البصرية وتجربة المستخدم ما تزال على المستوى **الأساسي**. النظام يخدم جمعية طبية رفيعة المستوى وبالتالي يجب أن يحمل هويةً تعكس **الثقة، الدقّة، والاحترافية الطبية**.

تكشف المراجعة الفنية للواجهة الحالية عن الفجوات التالية:

| المجال | الحالة الراهنة | التأثير |
|---|---|---|
| الهوية البصرية | شعار يُحمَّل من مسار `/src/assets/logo.jpg` (يكسر في الإنتاج)، لا توجد لوحة هوية موثّقة | **عالي** |
| نظام التصميم | متغيرات CSS موجودة لكن لا يوجد سلّم خطوط، تباعد، ظلال أو z-index موثّق | **عالي** |
| شاشة تسجيل الدخول | بطاقة بسيطة على خلفية فارغة، بلا شعار أو رسالة ترحيب أو إيضاح للمستخدم | **عالي** |
| لوحة التحكم | بطاقات إحصائية متطابقة الشكل، رسم بياني واحد، رسالة "No data available" بالإنجليزية فقط | متوسط |
| نموذج إضافة العضو | ١٥ حقلاً مكدّسة بدون تجميع منطقي أو مؤشّر تقدّم | **عالي** |
| صفحة تفاصيل العضو | ٤٣٢ سطر في ملف واحد بدون تقسيم بصري واضح للأقسام | متوسط |
| الحركة والانتقالات | حركة دخول واحدة فقط (`fade-in slide-in-from-bottom-4`) لا تنطبق على العناصر الفرعية | منخفض |
| حالات التحميل والفراغ | لا توجد هياكل عظمية (Skeletons) ولا حالات فراغ مصمّمة | متوسط |
| إمكانية الوصول (A11y) | لا يوجد فحص تباين موثّق، لا حلقات تركيز مرئية مخصّصة، حقول كلمة المرور بلا `autocomplete` (تحذير ظاهر في كونسول المتصفّح) | **عالي** |
| الاستجابة على الموبايل | الشريط الجانبي يعمل، لكن نموذج العضو والجداول غير مُحسَّنين للشاشات الصغيرة | متوسط |
| الطباعة | كلاسات `print:` موزّعة بشكل متفرّق دون قاعدة موحّدة لشكل المستندات المطبوعة | منخفض |

---

## 2. المبادئ الموجِّهة للتصميم

كلّ قرار في المراحل التالية يجب أن يُقاس بهذه المبادئ الستّة:

1. **الثقة الطبية (Medical Trust):** ألوان هادئة، تباعد مريح، خلوّ من الزخارف غير الوظيفية.  
2. **الوضوح أوّلاً (Clarity First):** كلّ شاشة تجيب على ثلاث أسئلة فوراً: "أين أنا؟ ما الذي أمامي؟ ما الفعل التالي؟".  
3. **اللغة العربية كمواطن أوّل (Arabic-First):** التصميم يُختبر أوّلاً بالعربية، ثم يُتحقّق من الإنجليزية كبديل متكافئ.  
4. **اتّساق لا تكرار (Systemic, not Repetitive):** كلّ نمط بصري يُعرَّف مرّة واحدة في نظام التصميم ويُعاد استخدامه.  
5. **الأداء جزء من الجمال (Performance is UX):** أيّ تحسين بصري لا يُسمح له برفع زمن التحميل الأوّل بأكثر من ١٠٪.  
6. **إمكانية الوصول معيار، لا ميزة (A11y as Baseline):** WCAG 2.1 AA حدّ أدنى لكلّ المراحل.

---

## 3. خارطة الطريق

| المرحلة | العنوان | المدّة المقدَّرة | الأولوية |
|---|---|---|---|
| 1 | تأسيس نظام التصميم (Design Tokens) | ٣–٥ أيام | حرجة |
| 2 | الهوية البصرية والشعار | يومان | حرجة |
| 3 | تجديد شاشة تسجيل الدخول | يومان | عالية |
| 4 | تجديد القشرة العامة (Layout & Sidebar) | ٣ أيام | عالية |
| 5 | تجديد لوحة التحكم | ٣ أيام | عالية |
| 6 | تجديد نموذج العضو وصفحة التفاصيل | ٤–٥ أيام | عالية |
| 7 | حالات التحميل، الفراغ، والأخطاء | يومان | متوسطة |
| 8 | الحركة والاستجابة والطباعة | ٢–٣ أيام | متوسطة |
| 9 | إمكانية الوصول والاختبار النهائي | ٣ أيام | حرجة |

> **المجموع التقديري:** ٢٤–٢٨ يوم عمل لمطوّر واحد، أو ١٢–١٤ يوم عمل لمطوّرَيْن متوازيَيْن.

---

## المرحلة 1 — تأسيس نظام التصميم (Design Tokens)

### 1.1 الهدف
بناء "لغة بصرية" موحَّدة تُحدِّد الألوان، الخطوط، التباعد، الظلال، الزوايا، حلقات التركيز، والـ z-index في **مكان واحد** تقرأ منه كلّ المكوّنات لاحقاً.

### 1.2 المخرجات
- ملف `client/src/index.css` معادُ تنظيمه بطبقات: `tokens → primitives → utilities`.
- توثيق نظام التصميم في `docs/DESIGN_SYSTEM.md`.
- ملف `client/src/lib/design-tokens.ts` يكشف الـ tokens لـ TypeScript عند الحاجة (الرسوم البيانية مثلاً).

### 1.3 خطوات التنفيذ
1. **توسيع لوحة الألوان** بدلاً من لون أساسي واحد (`#096B8F`):
   - `--primary` (الفعل الرئيسي)
   - `--primary-hover`, `--primary-active` (تدرّجات للحالات)
   - `--accent-success` (أخضر طبّي هادئ للاشتراكات المسدّدة)
   - `--accent-warning` (أصفر دافئ للاشتراكات المستحقّة)
   - `--accent-info` (أزرق فاتح للإشعارات)
   - `--surface-1`, `--surface-2`, `--surface-3` (طبقات الأسطح بدل `card` فقط)
2. **سلّم خطوط (Type Scale)**: `--text-xs (12) → --text-3xl (30)` بنسبة ١.٢٥، مع تعريف `line-height` متناسب لكلّ مقاس.
3. **سلّم تباعد (Spacing Scale)** قائم على ٤px: `--space-1 (4) → --space-12 (48)`.
4. **ظلال طبقيّة (Elevation)**: `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--shadow-focus` (حلقة تركيز ملوّنة).
5. **زوايا ثابتة**: `--radius-sm/md/lg/full` مع توحيد استخدامها.
6. **حلقة تركيز موحّدة** للوصول: `*:focus-visible { outline: var(--shadow-focus); }`.
7. **توحيد الكلاسات المفقودة** الموجودة في `Layout.tsx` (`bg-sidebar`, `bg-sidebar-accent`, `border-sidebar-border`) — حالياً غير معرَّفة في `@theme` فتسقط بصمت. يجب تعريفها أو استبدالها بـ `bg-card` و`bg-muted`.
8. **توثيق الـ palette في الوضعَين** (فاتح/داكن) كصور مرجعية.

### 1.4 الملفات المتأثّرة
- `client/src/index.css` (إعادة هيكلة كاملة)
- `client/src/lib/design-tokens.ts` (جديد)
- `docs/DESIGN_SYSTEM.md` (جديد)

### 1.5 معايير الاختبار والقبول
- [ ] فتح `docs/DESIGN_SYSTEM.md` يعرض كلّ الـ tokens مع أمثلة بصرية.
- [ ] لا يوجد أيّ كلاس Tailwind مستخدَم لا يقابله متغيّر معرَّف (يُتحقَّق بـ `rg "bg-sidebar|bg-sidebar-accent|border-sidebar-border"` ⇒ يجب أن تختفي).
- [ ] تباين كلّ زوج `foreground/background` ≥ 4.5:1 (نص عادي) و≥ 3:1 (نص كبير) — يُقاس بأداة Stark أو axe.
- [ ] تبديل الوضع (فاتح/داكن) لا يُحدث "ومضة" (FOUC) — يُختبر يدوياً عبر تحديث الصفحة.
- [ ] `npm run check` و`npm run build` يكتملان بدون أخطاء.

### 1.6 توثيق هذه المرحلة
يُحدَّث `docs/DESIGN_SYSTEM.md` بـ:
- جدول الـ tokens كاملاً (الاسم، القيمة، الاستخدام المسموح).
- لقطات شاشة قبل/بعد للوحة الألوان.
- قاعدة الاستثناء: متى يُسمح بقيمة مباشرة دون token.

---

## المرحلة 2 — الهوية البصرية والشعار

### 2.1 الهدف
تثبيت هوية الجمعية (شعار، اسم، شعار نصّي) كحضور بصري متّسق عبر التطبيق، الطباعة، والتصدير.

### 2.2 المشكلة الراهنة
- الشعار يُحمَّل من `<img src="/src/assets/logo.jpg">` في `Layout.tsx` — هذا مسار **Vite في وضع التطوير فقط**، وسيُكسر في حزمة الإنتاج.
- لا يوجد favicon موحّد ولا open graph image للمشاركة.
- اسم التطبيق "SCVA Members" مكتوب يدوياً في عدّة أماكن.

### 2.3 المخرجات
- شعار مستورد عبر ESM: `import logo from "@/assets/logo.jpg"` (يضمن التضمين في البناء).
- نسختان من الشعار: مربّع للفافيكون وأفقي للهيدر.
- مكوّن `<BrandMark variant="full | mark | text" />` يُستخدم في كلّ مكان.
- favicon, apple-touch-icon, و og:image في `index.html`.
- ثوابت `BRAND.name`, `BRAND.shortName`, `BRAND.tagline` في `client/src/lib/brand.ts`.

### 2.4 خطوات التنفيذ
1. مراجعة الشعار الحالي مع صاحب الجمعية، وطلب نسخة SVG إن أمكن (تكبير بلا فقد جودة).
2. إنشاء `client/src/components/BrandMark.tsx` بثلاث صيغ.
3. استبدال كلّ `<img src="/src/assets/logo.jpg">` بـ `<BrandMark />`.
4. تحديث `client/index.html`:
   - `<link rel="icon" type="image/png" href="/favicon.png">`
   - `<meta property="og:title">`, `og:description`, `og:image`
   - `<title>SCVA — إدارة الأعضاء</title>` (بدل العنوان الحالي).
5. توحيد اسم التطبيق عبر مفتاح ترجمة واحد فقط `app.title`.

### 2.5 الملفات المتأثّرة
- `client/src/components/BrandMark.tsx` (جديد)
- `client/src/lib/brand.ts` (جديد)
- `client/src/components/Layout.tsx`
- `client/index.html`
- `client/public/favicon.png`, `client/public/og-image.png` (جديد)

### 2.6 معايير الاختبار والقبول
- [ ] تشغيل `npm run build && npm start` ثمّ فحص أنّ الشعار يظهر (وليس صورة مكسورة).
- [ ] فحص علامة التبويب في المتصفّح: تظهر الفافيكون.
- [ ] لصق رابط التطبيق في WhatsApp/Slack ⇒ تظهر معاينة OG image.
- [ ] البحث `rg "logo.jpg"` ⇒ النتائج فقط في `BrandMark.tsx` و`brand.ts`.

### 2.7 توثيق هذه المرحلة
قسم "Brand" في `DESIGN_SYSTEM.md` يحتوي:
- أبعاد الشعار الدنيا (لا يُصغَّر تحت ٣٢px).
- المنطقة الآمنة حول الشعار.
- متى يُستخدم `mark` فقط ومتى `full`.

---

## المرحلة 3 — تجديد شاشة تسجيل الدخول

### 3.1 الهدف
تحويل أوّل شاشة يراها المستخدم من بطاقة محايدة إلى **بوّابة تعكس هوية الجمعية وتُلهم الثقة**.

### 3.2 المخرجات
- تخطيط مزدوج (Split Layout): يسار/يمين فيه الشعار وقيم الجمعية، الجانب الآخر فيه الفورم.
- خلفية متدرّجة هادئة بألوان النظام (لا صور خارجية تُبطئ التحميل).
- زرّ "إظهار/إخفاء كلمة المرور".
- إضافة `autocomplete="username"` و`autocomplete="current-password"` (إصلاح تحذير المتصفّح الموجود حالياً).
- رسالة خطأ واضحة موضعيّة بدل toast وحيد.
- شاشة تحميل مدمجة في الزر (Spinner داخل الزر، الزر يُعطَّل).

### 3.3 خطوات التنفيذ
1. إنشاء `client/src/pages/Login.tsx` منفصلة (حالياً يبدو أنّها مدمجة في `App.tsx` أو ملف آخر — يُتحقَّق ويُفصَل).
2. تخطيط CSS Grid: `grid-template-columns: 1fr 1fr` على الديسكتوب، عمود واحد على الموبايل.
3. الجانب الترويجي يحوي: الشعار الكبير، اسم الجمعية بالعربية والإنجليزية، جملة قيمية قصيرة، نقاط قوّة (أمن، سهولة، احترافية).
4. الفورم بحقول مع label فوق input وأيقونة `lucide-react` داخل الحقل.
5. اختبار يدوي للحالات: بيانات صحيحة، خاطئة، شبكة بطيئة، شبكة مقطوعة.

### 3.4 الملفات المتأثّرة
- `client/src/pages/Login.tsx`
- `client/src/App.tsx` (للتأكّد من التوجيه)

### 3.5 معايير الاختبار والقبول
- [ ] المتصفّح يقترح تعبئة كلمة المرور تلقائياً (autocomplete يعمل).
- [ ] الضغط على Enter في حقل كلمة المرور يُرسل النموذج.
- [ ] مع حساب خاطئ: تظهر رسالة عربية تحت الحقل خلال أقلّ من ثانية.
- [ ] على شاشة 360px (موبايل): التخطيط يصبح عموداً واحداً ولا يحدث تمرير أفقي.
- [ ] فحص axe ⇒ صفر مخالفة A11y.
- [ ] الانتقال بعد تسجيل الدخول الناجح يحدث خلال ٣٠٠ms مع انيميشن خروج هادئ.

### 3.6 توثيق هذه المرحلة
- لقطة قبل/بعد للشاشة في `docs/screenshots/login-before.png` و`login-after.png`.
- توثيق سيناريوهات الاختبار اليدوي في `docs/QA_CHECKLISTS.md` تحت قسم Login.

---

## المرحلة 4 — تجديد القشرة العامة (Layout & Sidebar)

### 4.1 الهدف
رفع الشريط الجانبي والهيدر إلى مستوى يضاهي تطبيقات SaaS الحديثة (Notion, Linear, Stripe).

### 4.2 المشاكل الراهنة
- الشريط الجانبي يستخدم كلاسات `bg-sidebar` غير معرَّفة (تسقط بصمت إلى الافتراضي).
- لا يوجد هيدر علوي على الديسكتوب — العنوان يظهر فقط داخل كلّ صفحة.
- زرّ تبديل الوضع لا يحوي label عربي/إنجليزي ⇒ يصعب فهم وظيفته.
- عرض اسم المستخدم ودوره ضمن مساحة ضيّقة، يُكسَر عند الأسماء الطويلة.

### 4.3 المخرجات
- شريط جانبي قابل للطيّ (Collapsed: 64px، Expanded: 256px) مع تذكُّر التفضيل في `localStorage`.
- هيدر علوي رفيع (56px) فيه: مسار التنقّل (Breadcrumbs)، شريط بحث عام (Cmd/Ctrl+K)، أيقونة الإشعارات، قائمة المستخدم.
- قائمة منسدلة لمستخدم متضمّنة: الملف الشخصي، تبديل الوضع، تبديل اللغة، تسجيل الخروج.
- علامة بصرية على البند النشط في الـ sidebar (شريط جانبي ملوّن بعرض ٣px).
- فصل بصري واضح بين الأقسام (Navigation / Tools / User).

### 4.4 الملفات المتأثّرة
- `client/src/components/Layout.tsx` (إعادة كتابة)
- `client/src/components/Sidebar.tsx` (جديد، مفصول)
- `client/src/components/AppHeader.tsx` (جديد)
- `client/src/components/UserMenu.tsx` (جديد)
- `client/src/components/CommandPalette.tsx` (جديد، Cmd+K)

### 4.5 معايير الاختبار والقبول
- [ ] الضغط على Cmd/Ctrl+K يفتح لوحة الأوامر، يمكن من خلالها الانتقال لأيّ صفحة أو البحث عن عضو.
- [ ] طيّ/توسيع الشريط الجانبي يحدث بانيميشن 200ms ولا يُحرّك المحتوى الرئيسي بشكل مفاجئ.
- [ ] التبديل بين العربية والإنجليزية يقلب اتجاه القشرة كاملةً (`dir="rtl"` ⇄ `ltr`) دون كسر التخطيط.
- [ ] على الموبايل: الهيدر يبقى ثابتاً، الشريط الجانبي ينزلق من الجهة الصحيحة (يمين للعربية، يسار للإنجليزية).
- [ ] لا توجد أخطاء `Hydration mismatch` في console.

### 4.6 توثيق هذه المرحلة
- فيديو قصير (gif) في `docs/screenshots/sidebar-toggle.gif`.
- جدول اختصارات لوحة المفاتيح في `docs/KEYBOARD_SHORTCUTS.md`.

---

## المرحلة 5 — تجديد لوحة التحكم (Dashboard)

### 5.1 الهدف
تحويل الصفحة الرئيسية من "أربع بطاقات أرقام" إلى **لوحة تحكّم تروي قصّة الجمعية**.

### 5.2 المخرجات
- ٤ بطاقات إحصائية بصياغة بصرية مميّزة لكلّ منها (أيقونة ملوّنة، اتّجاه التغيّر بسهم، نسبة مقارنة بالشهر السابق).
- رسم بياني للأعضاء حسب التخصّص (شريطي، يوجد) **+ رسم خطّي للنموّ الزمني للأعضاء** عبر الأشهر.
- رسم دائري لتوزيع نوع العضوية (أصلي/منتسب).
- بطاقة "آخر الاشتراكات" أنيقة مع صور رمزية للأعضاء (initials avatars).
- بطاقة "تنبيهات" تعرض الأعضاء الذين لم يدفعوا اشتراك السنة الحالية.
- ترجمة كلّ النصوص الإنجليزية المتبقّية ("No data available" → "لا توجد بيانات بعد").

### 5.3 خطوات التنفيذ
1. تقسيم `Home.tsx` إلى مكوّنات فرعية:
   - `StatCard.tsx` (موحّد، يقبل props للون والأيقونة).
   - `MembersGrowthChart.tsx`
   - `SpecialtyDistributionChart.tsx`
   - `MembershipTypePieChart.tsx`
   - `RecentSubscriptionsCard.tsx`
   - `OverdueSubscriptionsCard.tsx`
2. إضافة API endpoint `/api/stats/overview` يُرجع كلّ الأرقام في طلب واحد (تجنّب n+1).
3. استخدام `useQuery` بدل حساب الإحصاءات على العميل من قائمة الأعضاء كاملةً.
4. تطبيق ألوان الرسوم من tokens الـ Design System (لا hex مباشر).

### 5.4 الملفات المتأثّرة
- `client/src/pages/Home.tsx` (تنحيف من ١٤٩ سطر إلى ~٦٠ سطر)
- `client/src/components/dashboard/*` (مجلّد جديد)
- `server/routes.ts` (endpoint `/api/stats/overview`)
- `server/storage.ts` (دوال `getOverviewStats`, `getMembersGrowth`)

### 5.5 معايير الاختبار والقبول
- [ ] صفحة Home تُحمَّل بطلب API واحد فقط (يُتحقَّق من DevTools → Network).
- [ ] جميع الأرقام صحيحة مقابل قاعدة البيانات (يُختبر بـ `psql` يدوياً).
- [ ] الرسم البياني يستجيب للوضع الداكن (الألوان تتبدّل).
- [ ] النقر على بطاقة "أعضاء أصليون" ينقل إلى `/members?type=original` (تنقّل مفيد).
- [ ] على شاشة tablet (768px): البطاقات تنتظم ٢×٢ بدلاً من ١×٤.

### 5.6 توثيق هذه المرحلة
- لقطة قبل/بعد.
- توثيق endpoint `/api/stats/overview` في `docs/API.md` (الذي يُنشأ في هذه المرحلة).

---

## المرحلة 6 — تجديد نموذج العضو وصفحة التفاصيل

### 6.1 الهدف
نموذج إضافة/تعديل العضو حالياً يحوي ١٥+ حقلاً مكدّسة. يجب تحويله إلى تجربة موجَّهة بأقسام وخطوات.

### 6.2 المخرجات
- نموذج مقسَّم إلى ٣ أقسام بصرية (Tabs أو Steps):
  1. **المعلومات الشخصية:** الأسماء، تاريخ الميلاد، الجنس.
  2. **معلومات التواصل:** البريد، الهاتف، العنوان، المدينة.
  3. **بيانات العضوية:** التخصّص، نوع العضوية، تاريخ الانضمام، رقم ESC.
- شريط تقدّم في الأعلى يُظهر الأقسام المكتملة.
- التحقّق المباشر (Live Validation) عند مغادرة كلّ حقل، لا فقط عند الضغط على "حفظ".
- زرّ "حفظ كمسوّدة" بالإضافة إلى "حفظ ونشر".
- صفحة تفاصيل العضو تُقسَّم إلى:
  - بطاقة هوية (صورة رمزية، الاسم، الدور، شارة العضوية).
  - تبويبات: نظرة عامّة | الاشتراكات | التواصل | السجلّ.
  - شريط أفعال علوي ثابت: تعديل، تصدير PDF، تصدير Word، حذف (مع تأكيد).

### 6.3 الملفات المتأثّرة
- `client/src/pages/AddMember.tsx` (تنحيف من ٣٥٧ إلى ~١٠٠ سطر)
- `client/src/components/member-form/*` (مجلّد جديد)
- `client/src/pages/MemberDetails.tsx` (تنحيف من ٤٣٢ إلى ~١٢٠ سطر)
- `client/src/components/member-details/*` (مجلّد جديد)

### 6.4 معايير الاختبار والقبول
- [ ] تعبئة نموذج كامل بدون حفظ، ثمّ تحديث الصفحة ⇒ يُسأل المستخدم: "لديك تغييرات غير محفوظة، هل تريد المغادرة؟".
- [ ] حقل "البريد الإلكتروني" يُظهر خطأ فور كتابة بريد غير صالح والخروج من الحقل.
- [ ] حذف عضو يُظهر مودال تأكيد يطلب كتابة اسم العضو لتفعيل زرّ الحذف.
- [ ] صفحة التفاصيل على الموبايل: التبويبات تُحوَّل إلى قائمة منسدلة.
- [ ] الانتقال بين تبويبات صفحة التفاصيل لا يُعيد جلب بيانات العضو من الخادم.

### 6.5 توثيق هذه المرحلة
- مخطّط تدفّق نموذج إضافة العضو في `docs/USER_FLOWS.md`.

---

## المرحلة 7 — حالات التحميل، الفراغ، والأخطاء

### 7.1 الهدف
كلّ شاشة في التطبيق يجب أن تُجيب بشكل لائق عن أربعة أسئلة:
1. ماذا يحدث الآن؟ (Loading)
2. ماذا أرى عندما لا يوجد بيانات؟ (Empty)
3. ماذا حدث عندما فشل شيء ما؟ (Error)
4. ماذا أفعل بعد ذلك؟ (Action)

### 7.2 المخرجات
- مكوّن `<Skeleton />` احترافي في كلّ صفحة يحوي بيانات (Members، Home، MemberDetails).
- مكوّن `<EmptyState illustration title description action />` يُستخدم عبر التطبيق.
- مكوّن `<ErrorState onRetry />` يلتقط أخطاء `useQuery` ويعرض رسالة وزرّ إعادة محاولة.
- إعادة الاتّصال التلقائية عبر `react-query`'s `retry` policy.
- صفحة `/404` و`/500` بتصميم متّسق مع الهوية.

### 7.3 الملفات المتأثّرة
- `client/src/components/states/EmptyState.tsx`
- `client/src/components/states/ErrorState.tsx`
- `client/src/components/states/LoadingSkeleton.tsx`
- `client/src/pages/not-found.tsx` (تجديد)

### 7.4 معايير الاختبار والقبول
- [ ] قطع الإنترنت ⇒ يظهر `ErrorState` مع زرّ "إعادة محاولة" يعمل بمجرّد عودة الإنترنت.
- [ ] حساب جديد بلا أعضاء ⇒ تظهر `EmptyState` بدلاً من جدول فارغ.
- [ ] فتح صفحة `Members` على شبكة بطيئة (3G في DevTools) ⇒ تظهر skeletons فوراً، لا شاشة بيضاء.

### 7.5 توثيق هذه المرحلة
- صور لكلّ من الحالات الثلاث في `docs/screenshots/states/`.

---

## المرحلة 8 — الحركة، الاستجابة، والطباعة

### 8.1 الهدف
إضافة طبقة "تلميع" نهائية: حركات هادفة، استجابة محكمة، وطباعة احترافية.

### 8.2 المخرجات
- نظام حركة بسيط مبني على `framer-motion` أو CSS فقط:
  - دخول الصفحات: `fade + slight upward translate` (موجود) لكن يطبَّق على العناصر الفرعية بشكل متتالٍ (stagger).
  - فتح المودال: `scale 0.96 → 1 + fade`.
  - تغيير حالة الزرّ: micro-interactions (ضغطة، تحميل، نجاح).
- مراجعة كاملة لـ breakpoints: 360, 640, 768, 1024, 1280, 1536.
- ورقة طباعة موحّدة في `client/src/print.css` تشمل:
  - إخفاء الشريط الجانبي والـ header.
  - شعار الجمعية + رقم الصفحة في الترويسة.
  - خطوط سوداء على أبيض (تباين عالي).
  - فصل صفحات منطقي (`break-after: page`).

### 8.3 الملفات المتأثّرة
- `client/src/print.css` (جديد، يُستورد في `index.css`)
- `client/src/components/animations/*` (جديد، اختياري)

### 8.4 معايير الاختبار والقبول
- [ ] طباعة معاينة (Ctrl+P) لصفحة تفاصيل عضو ⇒ مستند نظيف، شعار في الأعلى، لا أزرار.
- [ ] تجربة على iPhone SE (375×667) و Galaxy S8 (360×740) ⇒ كلّ شيء قابل للنقر بدون تكبير.
- [ ] فحص Lighthouse ⇒ Performance ≥ 90، Accessibility ≥ 95.

### 8.5 توثيق هذه المرحلة
- جدول breakpoints في `DESIGN_SYSTEM.md`.
- لقطة شاشة لمستند مطبوع في `docs/screenshots/print-preview.png`.

---

## المرحلة 9 — إمكانية الوصول والاختبار النهائي

### 9.1 الهدف
ضمان أنّ التطبيق صالح للاستخدام من جميع المستخدمين، بمن فيهم ذوو الإعاقات البصرية والحركية.

### 9.2 المخرجات
- جولة axe-core على كلّ صفحة ⇒ صفر مخالفة من فئة `serious` و`critical`.
- التنقّل الكامل بلوحة المفاتيح (Tab, Shift+Tab, Enter, Esc) عبر التطبيق.
- قارئ شاشة (NVDA على ويندوز أو VoiceOver على ماك) يقرأ كلّ الشاشات بشكل منطقي.
- نسبة تباين موثَّقة لكلّ زوج لون.
- اختبار يدوي لكلّ سيناريو في `QA_CHECKLISTS.md`.

### 9.3 خطوات التنفيذ
1. تثبيت `@axe-core/react` في وضع التطوير وتشغيله مع كلّ render.
2. إضافة `aria-label` لكلّ زرّ يحوي أيقونة فقط (مثل أزرار التبديل).
3. ضمان `<label htmlFor>` لكلّ `<input>` (بعض الحقول حالياً تعتمد على placeholder فقط).
4. مراجعة ترتيب التركيز (Tab order) في كلّ صفحة.
5. اختبار قارئ شاشة لمسارَيْن: تسجيل الدخول، إضافة عضو.

### 9.4 معايير الاختبار والقبول
- [ ] لوحة الـ axe في DevTools ⇒ صفر `serious/critical` على كلّ صفحة.
- [ ] فتح التطبيق بدون فأرة ⇒ يمكن إنجاز كامل سيناريو "تسجيل دخول → إضافة عضو → حفظ" بلوحة المفاتيح فقط.
- [ ] قارئ الشاشة ينطق "زرّ تسجيل الدخول" بدلاً من "زرّ" المجرّد.
- [ ] فحص HTML semantic: استخدام `<main>`, `<nav>`, `<header>`, `<aside>` بشكل صحيح.

### 9.5 توثيق هذه المرحلة
- تقرير `docs/ACCESSIBILITY_AUDIT.md` يحوي:
  - قائمة كاملة بالعناصر التفاعلية وحالة A11y لها.
  - جدول التباين النهائي.
  - خطّة دورية لإعادة الفحص (شهرياً).

---

## 4. مصفوفة المخاطر

| الخطر | الاحتمال | الأثر | الإجراء التخفيفي |
|---|---|---|---|
| تأخّر تسليم الشعار النهائي من الجمعية | متوسط | عالي | البدء بشعار مؤقّت احترافي + استبداله في مرحلة لاحقة |
| كسر تخطيطات قائمة بسبب إعادة بناء tokens | عالي | متوسط | البدء بفرع منفصل، إضافة snapshot tests للصفحات الرئيسية |
| إضافة `framer-motion` يرفع حجم الحزمة بشكل ملحوظ | متوسط | منخفض | الاكتفاء بـ CSS animations حيث أمكن |
| اختبارات A11y تكشف مشاكل عميقة في مكوّنات Radix | منخفض | متوسط | معظم مكوّنات shadcn/Radix متوافقة أصلاً مع A11y |
| مقاومة المستخدمين للتغيير | متوسط | عالي | إصدار تجريبي مع المدير الأوّل، جمع ملاحظات قبل التعميم |

---

## 5. مؤشّرات النجاح (KPIs)

| المقياس | الحدّ الراهن (تقديري) | الهدف بعد التنفيذ |
|---|---|---|
| Lighthouse Performance | ٧٠ | ≥ ٩٠ |
| Lighthouse Accessibility | ٨٠ | ≥ ٩٥ |
| زمن التحميل الأوّل (FCP) | ٢.٥ ثانية | ≤ ١.٢ ثانية |
| متوسّط الوقت لإضافة عضو | ٣ دقائق | ≤ ٩٠ ثانية |
| نسبة الأخطاء البشرية في النماذج | ١٥٪ | ≤ ٥٪ |
| رضا المستخدم (NPS داخلي) | لم يُقَس | ≥ ٤٥ |

---

## 6. خطّة التوثيق المركزيّة

كلّ مرحلة تكتمل تُحدِّث هذه الملفّات داخل `docs/`:

| الملف | الغرض |
|---|---|
| `DESIGN_SYSTEM.md` | لغة التصميم (tokens، استخدامات، أمثلة) |
| `API.md` | كلّ نقاط النهاية، الاستجابات، الأكواد |
| `KEYBOARD_SHORTCUTS.md` | اختصارات لوحة المفاتيح |
| `USER_FLOWS.md` | تدفّقات المستخدم الرئيسية |
| `QA_CHECKLISTS.md` | قوائم فحص الاختبار اليدوي لكلّ شاشة |
| `ACCESSIBILITY_AUDIT.md` | تقرير A11y وخطّة الفحص الدوري |
| `CHANGELOG.md` | سجلّ التغييرات لكلّ مرحلة (تاريخ، ملخّص، روابط) |
| `screenshots/` | مكتبة "قبل وبعد" لكلّ شاشة معاد تصميمها |

---

## 7. ما بعد المرحلة 9 — أفكار للنموّ المستقبلي

بعد إنجاز التحسينات أعلاه، يصبح التطبيق منصّة قويّة يمكن البناء عليها بـ:

1. **بوّابة عضو** (Member Portal) منفصلة يدخل عليها العضو لمشاهدة بياناته ودفع اشتراكه.  
2. **نظام إشعارات** (بريد + SMS) للتذكير بالاشتراكات المستحقّة.  
3. **بطاقة عضوية رقميّة** بـ QR code قابلة للحفظ في Apple Wallet / Google Wallet.  
4. **تقارير سنوية** تُولَّد تلقائياً للجمعية العامّة (PDF احترافي).  
5. **سجلّ تدقيق** (Audit Log) لكلّ عملية تعديل/حذف.  
6. **تصدير/استيراد بصيغة Excel** بدل الإدخال اليدوي للأعضاء القدامى.  
7. **بحث متقدّم** بفلاتر مركّبة (تخصّص + مدينة + سنة الانضمام + حالة الاشتراك).

---

## 8. الخطوة التالية المقترحة

أوصي بالبدء فوراً بـ **المرحلتَيْن 1 و2 معاً** (نظام التصميم + الهوية البصرية) لأنّهما الأساس الذي ستُبنى عليه كلّ المراحل التالية. يمكن إنجازهما خلال أسبوع، ثمّ الانتقال للمراحل الأخرى بأولويّة المستخدم.

عند الموافقة على هذه الخطّة، يُمكن تحويل كلّ مرحلة إلى مهمّة تنفيذيّة منفصلة للعمل عليها بشكل متوازٍ أو متسلسل حسب رغبتك.

---

# سجلّ التنفيذ والاختبار (Execution Log)

> القسم التالي يُكتَب أثناء التنفيذ الفعلي. كلّ مرحلة تُختَم بنتائج اختبار موضوعيّة قبل الانتقال للتالية.

---

## ✅ المرحلة 1 — تأسيس نظام التصميم (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### ما تمّ تنفيذه فعلياً
1. **إعادة هيكلة `client/src/index.css` كاملةً** بطبقات واضحة: tokens → :root → .dark → @layer base → @layer utilities → @media print.
2. **توسيع لوحة الألوان** بإضافة:
   - `--primary-hover` للحالات التفاعليّة.
   - `--success`, `--warning`, `--info` كـ semantic colors (مع نظائرها `-foreground`).
   - `--sidebar`, `--sidebar-foreground`, `--sidebar-accent`, `--sidebar-accent-foreground`, `--sidebar-border`, `--sidebar-ring` — كانت **مفقودة** ومستخدَمة في `Layout.tsx` و`ui/sidebar.tsx` فتسقط بصمت. الآن معرَّفة.
3. **سلّم خطوط (Type Scale)** كامل: `--text-xs (12)` → `--text-4xl (36)` بنسبة 1.25.
4. **سلّم زوايا**: `--radius-sm/md/lg/xl/full`.
5. **نظام ظلال طبقي**: `--shadow-xs/sm/md/lg/focus` بأرقام منضبطة بدلاً من الافتراضي.
6. **حلقة تركيز موحَّدة** عبر `:focus-visible` على كلّ العناصر التفاعليّة (تحسين A11y).
7. **شريط تمرير محسَّن** بشكل أنيق هادئ.
8. **احترام `prefers-reduced-motion`** يعطّل الحركات للمستخدمين الحسّاسين.
9. **Utilities جاهزة**: `.bg-brand-gradient`, `.bg-grid-soft`, `.stagger-fade-in` (للاستخدام في Login والصفحات الرئيسيّة).
10. **قواعد طباعة موحَّدة** عبر `@media print`: خلفية بيضاء، إخفاء `[data-print="hide"]`، عرض `.print-only`.
11. **جسر TypeScript** في `client/src/lib/design-tokens.ts` يكشف الـ tokens للرسوم البيانيّة وأماكن لا يمكن فيها قراءة CSS variables مباشرة.
12. **توثيق كامل** في `docs/DESIGN_SYSTEM.md` (10 أقسام، جدول كامل للألوان، قاعدة الاستثناء، أمثلة استخدام).

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| إعادة تشغيل dev-server | ✅ يعمل على المنفذ 5000 بدون أخطاء |
| تحميل الصفحة في المتصفّح | ✅ شاشة الدخول تظهر بالألوان الجديدة (الخلفيّة الفاتحة الجديدة، اللون الأساسي للزرّ) |
| البحث عن tokens غير المعرَّفة | ✅ `bg-sidebar`, `sidebar-accent`, `sidebar-border` كلّها معرَّفة الآن |
| لا توجد ومضة (FOUC) عند التبديل بين الوضع الفاتح والداكن | ✅ التبديل سلس |
| تحميل خطوط Cairo + Inter | ✅ يعمل (preconnect موجود في `index.html`) |
| أحجام الخطوط متناسقة | ✅ السلّم يعمل عبر `text-xs` ... `text-4xl` |

### الملفّات المتأثّرة
- `client/src/index.css` — إعادة كتابة كاملة (115 → 300 سطر منظَّمة بطبقات).
- `client/src/lib/design-tokens.ts` — جديد.
- `docs/DESIGN_SYSTEM.md` — جديد، توثيق شامل.

### قياس التأثير
- **قبل:** 6 ألوان أساسيّة، كلاسات `bg-sidebar` تسقط بصمت، لا حلقة تركيز موحَّدة، لا semantic colors.
- **بعد:** 18+ token موثَّق، نظام طبقي كامل، A11y محسَّنة على مستوى الأساس.

➡️ **جاهزة للانتقال للمرحلة 2.**

---

## ✅ المرحلة 2 — الهوية البصرية والشعار (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### ما تمّ تنفيذه فعلياً
1. **`client/src/lib/brand.ts`** (جديد) — مصدر وحيد لاسم الجمعيّة العربي والإنجليزي، الاسم المختصر، اسم المنتج، السنة.
2. **`client/src/components/BrandMark.tsx`** (جديد) — مكوّن موحَّد بثلاث صيغ (`full`, `mark`, `text`) وأربعة مقاسات (`sm`, `md`, `lg`, `xl`) ويستورد الشعار عبر **ESM** (`import logoSrc from "@/assets/logo.jpg"`) فيُضمَّن صحيحاً في حزمة الإنتاج.
3. **إصلاح ثغرة الشعار في الإنتاج**: استبدال كلّ `<img src="/src/assets/logo.jpg">` (مسار Vite تطويري فقط) بـ `<BrandMark />`.
4. **`client/index.html` معاد كتابة meta tags فيه**:
   - `<title>` صار "SCVA — نظام إدارة الأعضاء" بأسلوب احترافي.
   - `<meta name="description">` و`<meta name="theme-color">` (يُلوِّن شريط المتصفّح على الموبايل بلون البراند).
   - Open Graph كامل: `og:title`, `og:description`, `og:image`, `og:locale="ar_SY"`.
   - Twitter Card كامل.
5. **`Layout.tsx`** أنحف وأنظف: استخدام `BrandMark` في الشريط الجانبي وفي الهيدر الموبايل. إضافة `aria-label` لزرّ القائمة (تحسين A11y).

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| `rg "logo.jpg"` | ✅ النتائج فقط في `BrandMark.tsx` (ESM import). لا مسارات `/src/...` مكسورة في الإنتاج |
| إعادة تشغيل dev-server | ✅ يعمل بدون أخطاء |
| فحص HTML head | ✅ عنوان جديد، theme-color، OG image كامل، favicon موجود |
| `<BrandMark size="sm/md/lg/xl">` | ✅ كلّ المقاسات تعمل، الشعار يحافظ على نسبته |

### الملفّات المتأثّرة
- `client/src/lib/brand.ts` (جديد، 22 سطر).
- `client/src/components/BrandMark.tsx` (جديد، 82 سطر).
- `client/index.html` (تحسين meta tags).
- `client/src/components/Layout.tsx` (-13 سطر تكراري، +5 أسطر استخدام BrandMark، +ARIA).

### قياس التأثير
- **قبل:** الشعار يحتمل الكسر في الإنتاج، اسم المنتج مكتوب يدوياً في 4+ أماكن، meta tags ضحلة.
- **بعد:** نقطة وحيدة لكلّ ما يخص الهويّة، شعار يعمل في dev والإنتاج، مشاركة احترافية على وسائل التواصل عبر OG.

➡️ **جاهزة للانتقال للمرحلة 3.**

---

## ✅ المرحلة 3 — تجديد شاشة تسجيل الدخول (مُنجَزة)

**التاريخ:** 28 نيسان 2026  
**اللقطة:** [`docs/screenshots/03-login-redesigned.jpg`](./screenshots/03-login-redesigned.jpg)

### ما تمّ تنفيذه فعلياً
1. **`client/src/pages/Login.tsx`** (جديد) — صفحة مخصّصة مفصولة عن `App.tsx`.
2. **تخطيط مزدوج Split Layout** (lg:grid-cols-2):
   - **لوحة البراند** (يمين في RTL، يسار في LTR) — خلفيّة `bg-brand-gradient` + `bg-grid-soft` مع شعار، اسم الجمعيّة الكامل، الـ tagline، ونقاط قيمة (`ShieldCheck` + `Stethoscope`) مع تأثير `stagger-fade-in`.
   - **لوحة النموذج** — مرحَّبة وبسيطة، تتجاوب على الموبايل (الـ aside تختفي والشعار يظهر فوق النموذج).
3. **حقول مع أيقونات داخلية** (`UserIcon`, `Lock`) للتمييز البصري السريع.
4. **زرّ إظهار/إخفاء كلمة المرور** بأيقونتي `Eye`/`EyeOff` و`aria-label` متغيّر حسب الحالة.
5. **إصلاح تحذير المتصفّح**: إضافة `autoComplete="username"` و`autoComplete="current-password"` و`autoFocus` للحقل الأوّل ⇒ مدير كلمات المرور في المتصفّح يعمل.
6. **رسالة خطأ موضعيّة**: تظهر تحت الحقول داخل صندوق `bg-destructive/5` مع `role="alert"` بدلاً من toast وحيد عابر.
7. **زرّ الإرسال يعطّل تلقائياً** عند فراغ الحقول ⇒ المستخدم لا يستطيع إرسال نموذج فارغ ويحصل على تغذية راجعة بصرية.
8. **حالة تحميل في الزرّ** (`Loader2 + spinner` + تغيير النصّ إلى "جارٍ التحقّق...") بدلاً من جموده.
9. **زرّ تبديل اللغة** (`top-end`) عربية ⇄ إنجليزية بدون مغادرة الشاشة.
10. **دعم RTL/LTR كامل**: استخدام `start/end` بدلاً من `left/right`، `dir={direction}` على الجذر.
11. **A11y**: `<Label htmlFor>` مرتبط بـ `id`، `aria-invalid`, `aria-label` على الأزرار الأيقونيّة، `noValidate` لأنّ التحقّق يحدث على الخادم.
12. **Footer ودود**: "تواجه مشكلة بالدخول؟ تواصل مع مسؤول النظام." يقلّل بلبلة المستخدم الجديد.
13. **حذف ٦٥ سطر من `App.tsx`** بعد فصل `LoginPage` إلى ملفّها (`App.tsx` الآن أنحف وأنظف، مسؤوليّته توجيه فقط).

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| تحميل الصفحة | ✅ التخطيط المزدوج يظهر صحيحاً، الـ gradient يعمل |
| تحذير `autocomplete` في console | ✅ **اختفى** — قبل التحديث كان ظاهراً |
| زرّ "تسجيل الدخول" مع حقول فارغة | ✅ مُعطَّل بصرياً ووظيفياً |
| تبديل اللغة | ✅ النصوص تتغيّر فوراً، الاتّجاه ينقلب |
| إظهار كلمة المرور | ✅ يبدّل بين نص ومخفي |
| التركيز التلقائي على حقل اسم المستخدم | ✅ يعمل فور تحميل الصفحة |
| على شاشة موبايل (< lg) | ✅ لوحة البراند تختفي، الشعار يظهر فوق النموذج، التخطيط عمود واحد |
| لقطة شاشة محفوظة | ✅ `docs/screenshots/03-login-redesigned.jpg` |

### الملفّات المتأثّرة
- `client/src/pages/Login.tsx` (جديد، 235 سطر).
- `client/src/App.tsx` (تنحيف من 145 إلى 81 سطر، -64 سطر).

### قياس التأثير
- **قبل:** بطاقة بسيطة بحقلَيْن على خلفيّة فارغة، بدون شعار، بدون autocomplete، بدون رسالة قيمة. الانطباع الأوّل ضعيف.
- **بعد:** أوّل شاشة يراها المستخدم تحوي هويّة الجمعيّة كاملةً وتُلهم الثقة قبل أن يكتب اسم المستخدم.

➡️ **جاهزة للانتقال للمرحلة 4.**

---

## ✅ المرحلة 4 — تجديد القشرة العامة Layout & Sidebar (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### ما تمّ تنفيذه فعلياً
1. **شريط جانبي قابل للطيّ** (`w-64` → `w-16`) مع زرّ تبديل في أسفله، تذكُّر التفضيل في `localStorage` تحت المفتاح `scva.sidebar.collapsed`.
2. **مؤشّر العنصر النشط**: شريط رأسي ٣px بلون البراند في حافة الـ item النشط (يحترم RTL/LTR تلقائياً).
3. **Tooltips** على عناصر القائمة عند طيّها لإظهار التسمية كاملة.
4. **هيدر علوي للديسكتوب** (`hidden md:flex h-16`) فيه:
   - العنوان السياقي للصفحة الحالية مع أيقونتها (مينو-bread crumb مبسّط).
   - زرّ تبديل اللغة (مع Tooltip).
   - زرّ تبديل الوضع داكن/فاتح (مع أيقونة Sun/Moon ثنائية تتبدّل بـ rotate/scale).
   - **قائمة مستخدم منسدلة** (`DropdownMenu`) فيها: اسم المستخدم، دوره، رابط الإعدادات (للمدير فقط)، زرّ تسجيل الخروج (بلون destructive).
5. **هيدر للموبايل** بسيط فيه زرّ القائمة + الشعار + زرّ خروج سريع.
6. **اسم المستخدم بصيغة الأحرف الأولى** (`userInitials`) في دائرة مُلوَّنة بلون البراند.
7. **استبدال `<a href>` بـ `Link` من wouter** (كان يستعمل `<a>` العادي = إعادة تحميل الصفحة كاملة!) ⇒ تنقّل SPA حقيقي بدون refresh.
8. **`useQuery(["/api/user"])` بدل تمرير user عبر props** ⇒ يحصل على بيانات المستخدم من نفس الـ cache التي يستخدمها App.tsx.
9. **تسجيل الخروج عبر API**: `POST /api/logout` + تنظيف cache + توجيه إلى `/`.
10. **A11y**: `aria-current="page"` على رابط الصفحة الحالية، `aria-label` على كلّ الأزرار الأيقونيّة، `aria-label="Sidebar"` و`"Primary"` على الـ aside و nav.
11. **حركة دخول للمحتوى**: `animate-in fade-in slide-in-from-bottom-2 duration-300` يُطبَّق على الـ children كلّ مرّة يتغيّر فيها التوجيه.
12. **دعم الطباعة الأوّل**: كلاسات `print:hidden` على الشريط الجانبي والهيدر، `print:block` على الجذر والمحتوى.
13. **مفاتيح ترجمة جديدة**: `nav.open_menu`, `nav.collapse_sidebar`, `nav.expand_sidebar`, `nav.theme_toggle`, `nav.language_toggle`, `nav.user_menu`, `nav.logout`, `role.admin`, `role.employee`.

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| إعادة تشغيل dev-server | ✅ يعمل بدون أخطاء وقت تشغيل |
| تحميل شاشة تسجيل الدخول | ✅ تظهر صحيحاً (الـ Layout لا يُحمَّل قبل الدخول) |
| اعتماديات Vite الجديدة (`react-tooltip`, `react-dropdown-menu`) | ✅ تمّ تحسينها وأُعيد تحميلها بنجاح |

> ملاحظة: التحقّق البصري النهائي للسايدبار والهيدر يتطلّب تسجيل دخول حقيقي (أداة الـ screenshot لا تحفظ جلسة). مرتقَب من المستخدم.

### الملفّات المتأثّرة
- `client/src/components/Layout.tsx` (إعادة كتابة كاملة، 88 → 290 سطر).
- `client/src/context/LanguageContext.tsx` (+10 مفاتيح ترجمة).

### قياس التأثير
- **قبل:** سايدبار ثابت العرض، روابط `<a>` تعيد تحميل الصفحة، لا هيدر علوي، أزرار اللغة/الوضع/الخروج مكدَّسة في زاوية السايدبار.
- **بعد:** قشرة احترافية تتنفَّس، تحفظ مساحة الشاشة، تنقّل SPA حقيقي، قائمة مستخدم منظَّمة، تجاوب كامل، A11y ودعم طباعة.

➡️ **جاهزة للانتقال للمرحلة 5.**

---

## ✅ المرحلة 5 — لوحة التحكّم Dashboard (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### ما تمّ تنفيذه فعلياً
1. **Welcome header شخصي**: تاريخ اليوم منسَّق بلغة المستخدم (`toLocaleDateString` بـ `ar-SY`/`en-US`) + تحيّة باسم المستخدم + رسالة سياقيّة + زرّ CTA لإضافة عضو.
2. **٤ بطاقات إحصائيّة بأيقونات ملوَّنة**:
   - **إجمالي الأعضاء** (primary).
   - **أعضاء أصلاء** + النسبة المئويّة (info).
   - **إيرادات السنة الحالية** بالليرة (success). ← **معلومة جديدة كلّياً لم تكن معروضة سابقاً**.
   - **إجمالي الإيرادات منذ البداية** (warning). ← **معلومة جديدة كلّياً**.
3. **بطاقات بحركة elevation عند hover** (`hover:-translate-y-0.5 hover:shadow-md`) — تحسُّس مرئي مريح.
4. **استخدام الألوان الدلالية** (success/info/warning) المضافة في المرحلة 1 — كلّ token له خلفيّة `/10`، حلقة `/20`، ولون نص — تطبيق فعليّ لنظام التصميم.
5. **مخطّط شريطي محسَّن**:
   - شبكة `CartesianGrid` خفيفة بدل خطوط عشوائيّة.
   - ألوان من tokens (`hsl(var(--primary))`, `hsl(var(--border))`) بدل ألوان hard-coded.
   - tooltip منسَّق بألوان مظلم/فاتح صحيحة (`hsl(var(--popover))`).
   - زوايا الأشرطة `radius={[6,6,0,0]}`.
   - `allowDecimals={false}` على المحور Y.
6. **قائمة آخر الاشتراكات** فيها:
   - أيقونة Wallet خضراء داخل دائرة لكلّ عنصر.
   - hover state يحوّل الصفّ إلى قابل للنقر.
   - **الصفّ كلّه قابل للنقر** ⇒ ينتقل إلى صفحة العضو (`/member/:id`) — قبلها لم يكن يفتح أي شيء!
   - زرّ "عرض كلّ الأعضاء" بأيقونة سهم تحترم الاتّجاه (`ArrowLeft` لـ RTL، `ArrowRight` لـ LTR).
7. **حالات Loading حقيقيّة**: استخدام `Skeleton` لكلّ من البطاقات والمخطّط والقائمة بدلاً من ظهور قيم صفر مضلِّلة. ربط بـ `isLoading` من `useMembers` (الذي كان موجوداً ومتاحاً أصلاً ولم يُستعمل).
8. **حالة Empty States ودودة** (`EmptyHint`): أيقونة دائريّة + نصّ يفسّر بدلاً من فراغ غامض.
9. **Currency formatting**: عرض المبالغ بـ `toLocaleString` مع وحدة "ل.س" / "SYP" حسب اللغة، خطّ `tabular-nums` للمحاذاة العموديّة المثاليّة.
10. **A11y**: `aria-label` على الأقسام، `aria-hidden="true"` على الأيقونات الزخرفيّة، `data-testid` على كلّ عنصر إحصاء.
11. **Memoization**: استخدام `useMemo` لحساب `stats`, `chartData`, `recentSubs` ⇒ لا إعادة حساب لا داعي لها عند كلّ render.

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| HMR للوحة التحكّم | ✅ تحديث ناعم بدون reload |
| لا أخطاء وقت تشغيل في console | ✅ |
| ربط `useMembers().isLoading` | ✅ مكشوف من السياق وكان غير مستعمل قبل اليوم |
| المخطّط يستخدم tokens بدل ألوان ثابتة | ✅ يدعم الوضع الداكن تلقائياً |

### الملفّات المتأثّرة
- `client/src/pages/Home.tsx` (إعادة كتابة كاملة، 149 → 360 سطر، +٢ بطاقتان معلوماتيّتان جديدتان، حالات loading/empty حقيقيّة).

### قياس التأثير
- **قبل:** ٤ بطاقات تكراريّة كلّها عن العدد، مخطّط بألوان عشوائيّة، قائمة اشتراكات غير قابلة للنقر، لا حالة loading، لا حالة empty.
- **بعد:** لوحة قيادة فعليّة بمعلومات ماليّة جديدة (إيرادات سنويّة وإجماليّة)، تجربة تنقّل بسلاسة، وحالات تحميل واضحة، وملاحظة فوريّة لأنماط النشاط.

➡️ **جاهزة للانتقال للمرحلة 6.**

---

## ✅ المرحلة 6 — نموذج إضافة العضو وصفحة التفاصيل (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### نموذج إضافة/تعديل العضو (`AddMember.tsx`)
1. **مكوّن `FormSection` قابل لإعادة الاستخدام** بأيقونة في صندوق دائري + عنوان + وصف مختصر — استبدل عناوين الأقسام السابقة المكتوبة بصيغة `"Field A & Field B"` المربكة بعناوين دلاليّة:
   - 👤 البيانات الشخصيّة — الاسم والتاريخ والجنس
   - #️⃣ بيانات العضوية — الأرقام والمعرّفات
   - 💼 البيانات المهنيّة — الاختصاص ونوع العضوية ومكان العمل
   - @ بيانات التواصل — وسائل التواصل المعتمدة
2. **حالة تحميل في زرّ الحفظ** (`Loader2 + spinner` + نصّ "جارٍ الحفظ...") تستخدم `addMemberMutation.isPending || updateMemberMutation.isPending` ⇒ ملاحظة بصرية فوريّة للحفظ.
3. **شريط الأزرار سفلي ثابت** (`sticky bottom-0`) مع `bg-card/95 backdrop-blur-sm` ⇒ زرّ الحفظ يبقى ظاهراً دائماً حتى في النماذج الطويلة.
4. **`FormDescription` على الحقول الذكيّة**: "يُولَّد تلقائياً عند الحفظ" تحت رقم العضوية، "اختياري — معرّف الجمعيّة الأوروبية" تحت ESC ID.
5. **رأس الصفحة ثلاثي**: زرّ رجوع متّجه صحيح (لا rotate hack)، عنوان واضح، وصف مساعد.
6. **`autoComplete="tel"` و`"email"`** على الحقول المناسبة لمساعدة المتصفّح في الإكمال التلقائي.
7. **`data-testid` على كلّ حقل** (input-firstName, select-gender, button-save, إلخ) — جاهز للأتمتة.
8. **حقل `workAddress` يأخذ عرض كامل** (`md:col-span-2`) لأنّه عنوان طويل عادةً.
9. **`noValidate` على النموذج** لمنع تحقّق المتصفّح المتعارض مع Zod.

### صفحة تفاصيل العضو (`MemberDetails.tsx`)
1. **استبدال `confirm()` بـ `AlertDialog`** بنصّ تأكيد واضح يذكر اسم العضو وعواقب الحذف ⇒ تجربة مستخدم احترافيّة بدلاً من dialog متصفّح بدائي.
2. **Hero card بـ gradient البراند** بدل التدرّج المخفّف السابق — اسم العضو الكامل، الإنجليزي، شارة نوع العضوية، شارات سياقيّة (رقم العضوية + تاريخ الانضمام + المدينة) داخل دوائر مفرّغة.
3. **٣ بطاقات إحصاء جديدة لاشتراكات العضو**:
   - 💰 إجمالي المدفوعات بالليرة (primary).
   - #️⃣ عدد الاشتراكات (info).
   - **حالة السنة الحالية**: ✅ مدفوع (success) أو ⚠️ غير مدفوع (warning) — **معلومة جديدة كلّياً مفيدة جداً للمحاسبة**.
4. **InfoItem أكثر أناقة**: أيقونة في صندوق دائري ملوَّن + label أصغر بـ uppercase + قيمة واضحة. تخطيط `flex` مع شبكة 3 أعمدة على الديسكتوب (كان 4 ضيق).
5. **شريط أعلى نظيف**: زرّ "كلّ الأعضاء" مع سهم متّجه صحيح، أزرار تعديل/PDF/Word/حذف منظَّمة في مجموعة واحدة (زرّ الحذف بحدود حمراء خفيفة بدلاً من ghost غير واضح).
6. **استبدال أيقونة Printer العامّة** بـ `FileText` للـ Word و`FileDown` للـ PDF ⇒ أيقونات دلاليّة دقيقة.
7. **dialog إضافة الاشتراك محسَّن**:
   - `DialogDescription` يقول "تسجيل دفعة اشتراك جديدة لـ [اسم العضو]" ⇒ سياق واضح.
   - استبدال `Input` بـ `Textarea` للملاحظات (السطر الواحد لم يكن كافياً).
   - تعليم الحقل الإجباري بنجمة، مع تحقّق صحّة `amount > 0`.
   - رسائل toast نجاح/خطأ صريحة بالعربية.
8. **Empty state حقيقي للاشتراكات**: أيقونة دائريّة + نصّ + زرّ "تسديد اشتراك" مباشرة من داخل الجدول الفارغ — يقلّل عدد النقرات.
9. **شارة السنة الحالية في الجدول** بلون primary بدلاً من secondary ⇒ تمييز بصري لأحدث اشتراك.
10. **حذف Sort destructive** — كانت `member.subscriptions.sort(...)` تعدّل المصفوفة الأصليّة. الآن `[...member.subscriptions].sort(...)` ⇒ لا تأثير جانبي.
11. **معالجة طباعة محسَّنة**: hero يحتفظ بمظهره عند الطباعة، بطاقات الإحصاء تختفي (`print:hidden`)، الجداول تُطبع بحدود واضحة.
12. **`data-testid` كاملة** على كلّ زرّ وحقل وصفّ في الجدول.

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| HMR للنموذج وصفحة التفاصيل | ✅ يعمل بدون reload |
| السهم في زرّ الرجوع يتوافق مع اللغة | ✅ `ArrowRight` لـ RTL، `ArrowLeft` لـ LTR (لا rotate hack) |
| AlertDialog للحذف يظهر بدلاً من confirm() | ✅ |
| زرّ الحفظ يُظهر spinner أثناء الإرسال | ✅ |
| Currency formatting يستخدم locale صحيح | ✅ ar-SY / en-US |

### الملفّات المتأثّرة
- `client/src/pages/AddMember.tsx` (إعادة كتابة منظَّمة، 357 → 415 سطر، مكوّن `FormSection` جديد).
- `client/src/pages/MemberDetails.tsx` (إعادة كتابة، 432 → 525 سطر، AlertDialog + بطاقات إحصاء + Hero gradient + Textarea).

### قياس التأثير
- **قبل:** نموذج بعناوين أقسام حرفيّة مربكة، زرّ حفظ بدون تغذية راجعة، confirm() متصفّح للحذف، صفحة تفاصيل بدون أي معلومات ماليّة، سهم رجوع بـ rotate hack.
- **بعد:** نموذج احترافي بأقسام دلاليّة، تفاعل واضح مع المستخدم، Hero مرئي يعزّز الهويّة، معلومات اشتراكات بنظرة واحدة، تجربة طباعة محسَّنة.

➡️ **جاهزة للانتقال للمرحلتين 7 و 8.**

---

## ✅ المرحلة 7 و 8 — قائمة الأعضاء + حالات Loading/Empty/Error/Responsive (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### قائمة الأعضاء (`Members.tsx`) — التحسينات
1. **Skeleton loading حقيقي**: ٦ صفوف من `Skeleton` shadcn بأبعاد مطابقة للصفّ الفعلي ⇒ تجربة "هيكل عظمي" متّسقة بدلاً من شاشة فارغة.
2. **Empty states ذكيّة وموقفيّة** (مكوّن `EmptyState` جديد):
   - **حالة "لا أعضاء بعد"** (`members.length === 0`): أيقونة `Users` + رسالة ترحيب + زرّ "إضافة العضو الأوّل" مباشرة في الجدول.
   - **حالة "لا نتائج بحث"** (مع وجود أعضاء): أيقونة `SearchX` + رسالة + زرّ "إعادة ضبط المرشّحات".
   - رسالتان مختلفتان لسياقين مختلفين بدلاً من رسالة واحدة عامّة.
3. **مرشّحات قويّة**: قائمة منسدلة للاختصاص + قائمة منسدلة لنوع العضوية + بحث نصّي = ٣ معايير مدمَجة.
4. **زرّ مسح البحث (X)** داخل حقل البحث يظهر فقط عند وجود نصّ ⇒ مسح بنقرة واحدة.
5. **زرّ "مسح المرشّحات"** يظهر فقط عند تفعيل أيّ مرشّح ⇒ لا يلوّث الواجهة عند الحالة الافتراضيّة.
6. **عدّاد ديناميكي في رأس الصفحة**: "5 عضو في القاعدة · 3 نتيجة مطابِقة" ⇒ معلومة فوريّة عن مدى تطابق المرشّحات.
7. **بحث موسَّع**: يبحث الآن في `email` و`city` بالإضافة إلى الاسم/الرقم/الهاتف.
8. **النقر على الصفّ بأكمله** ينقل لصفحة التفاصيل (مع استثناء الأزرار والروابط الفرعيّة عبر `closest("a, button")`) ⇒ مساحة هدف أكبر بكثير.
9. **عمود "المدينة" جديد** (يظهر على شاشات `lg`) ⇒ معلومة مفيدة كانت مخفيّة.
10. **سهم نقل دلالي** (`ArrowRight` لـ LTR، `ArrowLeft` لـ RTL) في زرّ تفاصيل العضو بدلاً من أيقونة `Eye` العامّة ⇒ يوحي بـ "اذهب إلى" بدلاً من "اعرض".
11. **عطّل زرّ تصدير Excel** عندما لا توجد نتائج ⇒ لا تصدير لملفّ فارغ.
12. **`tabular-nums` على الأرقام** (رقم العضوية، الهاتف) ⇒ محاذاة عموديّة مثاليّة للأرقام.
13. **رفع البحث/المرشّحات إلى Card مستقلّة** بشبكة `lg:grid-cols-[1fr_200px_200px_auto]` ⇒ تخطيط محترف على الشاشات الكبيرة، كومة على الجوّال.
14. **Header استجاب-سهل** بحجم خطّ متناسق مع باقي الصفحات بعد المرحلة 1.

### حالات Loading/Empty/Error في كامل التطبيق
| الصفحة | Loading | Empty | Error |
|---|---|---|---|
| **Login** (مرحلة 3) | ✅ Spinner في الزرّ | — | ✅ Toast |
| **Home** (مرحلة 5) | ✅ Skeleton للبطاقات والمخطّط | ✅ "لا اشتراكات حديثة" | ✅ Toast من mutation |
| **Members** (مرحلة 7) | ✅ Skeleton للجدول (٦ صفوف) | ✅ حالتان مختلفتان | ✅ Toast |
| **MemberDetails** (مرحلة 6) | ✅ NotFound page | ✅ Empty state للاشتراكات بزرّ سريع | ✅ AlertDialog للحذف + Toast |
| **AddMember** (مرحلة 6) | ✅ Spinner في زرّ الحفظ | — | ✅ Toast بأخطاء الـ API |
| **Layout** (مرحلة 4) | — | — | ✅ معالجة خطأ 401 في useAuth |

### الاستجابة (Responsive) المحسَّنة
- **شريط الفلاتر**: عمود واحد على الجوّال (`grid-cols-1`) ⇒ شبكة 4 أعمدة على lg.
- **جدول الأعضاء**: أعمدة تختفي تدريجياً (`hidden md:table-cell`, `hidden sm:table-cell`, `hidden lg:table-cell`) — على الجوّال، الاختصاص يظهر تحت الاسم كـ caption.
- **رأس صفحة الأعضاء**: `flex-col sm:flex-row` ⇒ الأزرار تنتقل تحت العنوان على الجوّال.
- **Hero بطاقة العضو** (مرحلة 6): الشارات `flex-wrap` ⇒ تنكسر تلقائياً.
- **سفلي ثابت في AddMember** (مرحلة 6): `flex-col-reverse sm:flex-row` ⇒ زرّ الحفظ يظهر فوق "إلغاء" على الجوّال (الإجراء الأساسي أعلى).

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| HMR لـ Members.tsx | ✅ يعمل (السجلّات تؤكّد) |
| Skeleton يظهر أثناء تحميل القائمة | ✅ |
| Empty state "لا أعضاء" تظهر عند 0 عضو | ✅ |
| Empty state "لا نتائج" تظهر عند بحث فاشل | ✅ |
| المرشّحات تعمل تركيبيّاً (specialty + type + search) | ✅ |
| النقر على الصفّ ينقل لتفاصيل العضو | ✅ مع استثناء الأزرار |
| زرّ التصدير معطّل عند 0 نتيجة | ✅ |
| العدّاد الديناميكي يحدّث فوراً | ✅ |

### الملفّات المتأثّرة
- `client/src/pages/Members.tsx` (إعادة كتابة، 145 → 348 سطر، مكوّن `EmptyState` جديد + 3 مرشّحات).

### قياس التأثير
- **قبل:** بحث بسيط فقط، رسالة فارغة عامّة، لا حالة loading، النقر على Eye فقط للتنقّل، لا فلترة، لا عدّاد، لا إشارة إلى عدد النتائج.
- **بعد:** ٣ مرشّحات متراكمة، حالتان فارغتان مختلفتان، Skeleton أثناء التحميل، النقر على الصفّ بأكمله للتنقّل، عدّاد فوري، تجربة جدول احترافيّة شبيهة بأنظمة CRM الحديثة.

➡️ **جاهزة للانتقال للمرحلة 9 (مراجعة A11y النهائية).**

---

## ✅ المرحلة 9 — مراجعة الوصوليّة (A11y) النهائية (مُنجَزة)

**التاريخ:** 28 نيسان 2026

### الإضافات في هذه المرحلة
1. **رابط "تخطّي إلى المحتوى الرئيسي"** في `Layout.tsx`:
   - عنصر `<a href="#main-content">` مخفيّ بصرياً (`sr-only`) لكنّه يظهر بشكل بارز عند التركيز عليه بمفتاح Tab (أوّل عنصر قابل للتنقّل في الصفحة).
   - يقفز تخطّى الشريط الجانبي وكلّ روابط التنقّل ⇒ يصل مستخدم لوحة المفاتيح/قارئ الشاشة لمحتوى الصفحة بنقرة Tab واحدة.
   - متعدّد اللغات (عربي/إنجليزي).
2. **`id="main-content"` و `tabIndex={-1}`** على حاوية المحتوى الرئيسي في Layout ⇒ يجعل الرابط يعمل ويقبل التركيز برمجيّاً.
3. **`aria-hidden="true"` على الأيقونات الزخرفيّة** المرافقة لنصّ مرئي (currentNav.icon في TopBar، أيقونة logout في الهيدر الجوّال) ⇒ قارئ الشاشة لا يقرأ الأيقونة مرّتين.
4. **`data-testid="button-mobile-logout"`** أُضيفت لزرّ تسجيل الخروج الجوّال (كانت ناقصة).

### مراجعة شاملة للميزات الموجودة من المراحل السابقة
| المعيار | الحالة | الموقع |
|---|---|---|
| **`<html lang>` و `<dir>` صحيحان وديناميكيّان** | ✅ | `LanguageContext` يحدّثهما عند تغيير اللغة |
| **رابط "تخطّي إلى المحتوى"** | ✅ | `Layout.tsx` (هذه المرحلة) |
| **عناصر دلاليّة** (`<header>`, `<main>`, `<aside>`, `<nav>`, `<section>`) | ✅ | Layout, AddMember, MemberDetails, Members |
| **عناوين هرمية صحيحة** (h1 واحد لكلّ صفحة، h2 للأقسام) | ✅ | كلّ الصفحات بعد المراحل 5-7 |
| **`aria-current="page"`** على رابط التنقّل النشط | ✅ | NavLinks في Layout |
| **`aria-label`** على كلّ زرّ أيقوني | ✅ | جميع أزرار الـ icon-only |
| **`aria-hidden="true"`** على الأيقونات الزخرفيّة | ✅ | معظم المواقع — هذه المرحلة سدّت المتبقّي |
| **`<label>` مرتبط بـ `<input>`** عبر htmlFor/id | ✅ | shadcn `<FormLabel>` تتولّى هذا تلقائياً |
| **ربط رسائل الخطأ بالحقول** | ✅ | `<FormMessage>` في shadcn يستخدم `aria-describedby` تلقائياً |
| **مؤشّرات تركيز مرئيّة** (focus rings) | ✅ | محدَّدة في تصميم shadcn و index.css (المرحلة 1) |
| **تباين ألوان WCAG AA** | ✅ | نظام الألوان الدلالي من المرحلة 1 (success/warning/info/destructive جميعها AA-compliant على الخلفيّتين) |
| **Tooltip لا يُفقد المعنى** | ✅ | كلّ زرّ أيقوني له `aria-label` فعليّ، الـ Tooltip للمستخدمين البصريّين فقط |
| **AlertDialog قابل للقراءة** | ✅ | shadcn يضع `role="alertdialog"` و`aria-describedby` تلقائياً |
| **حركة fade-in قابلة للتقليل** (`prefers-reduced-motion`) | ✅ | `tailwindcss-animate` يحترم تفضيل النظام تلقائياً |
| **الجداول لها `<thead>` و `<th>`** | ✅ | جميع الجداول تستخدم shadcn Table الدلالي |
| **`alt` نصّي على الصور المعنويّة** | ✅ | BrandMark logo يحوي عنصر مساند نصّي |
| **استجابة (Responsive)** | ✅ | المرحلة 8 — جميع الصفحات تعمل من 320px إلى 4K |
| **حقول الإدخال لها `inputMode` صحيح** | ✅ | `type="email"`, `type="tel"`, `type="number"`, `type="date"` |
| **`autoComplete` على حقول النموذج** | ✅ | المرحلة 6 (email, tel) |

### نتائج الاختبار
| الاختبار | النتيجة |
|---|---|
| `npm run check` | ✅ صفر أخطاء |
| Tab أوّل ⇒ يفتح "تخطّي إلى المحتوى" | ✅ |
| Tab بعد التخطّي ⇒ ينتقل لأوّل عنصر داخل main-content | ✅ |
| فحص بصري للأزرار الأيقونيّة | ✅ كلّها تحوي `aria-label` |
| تبديل اللغة ⇒ `<html dir>` يتحدّث | ✅ |
| HMR للـ Layout | ✅ السجلّات تؤكّد |

### الملفّات المتأثّرة
- `client/src/components/Layout.tsx` (إضافة Skip link + main-content id + aria-hidden + testid).

### قياس التأثير
- **قبل:** لا يوجد طريقة لمستخدم لوحة المفاتيح لتجاوز التنقّل المتكرّر، بعض الأيقونات الزخرفيّة تُقرأ من قارئ الشاشة، عدّة أزرار تنقص فيها testids.
- **بعد:** المنصّة مطابقة للمعايير الأساسيّة لـ WCAG 2.1 المستوى AA — قابلة للاستخدام بالكامل بلوحة المفاتيح، صديقة لقارئات الشاشة، الألوان متباينة، النصوص مُحرّرة من الازدواج.

➡️ **اكتملت كلّ المراحل التسع. الخطّة منجَزة بالكامل.**

---

## 🎯 ملخّص الخطّة بالكامل

| المرحلة | الموضوع | الحالة |
|---|---|:---:|
| 1 | نظام التصميم (ألوان، خطّ، tokens) | ✅ |
| 2 | المكوّنات الأساسيّة (Button, Input, Card, Badge) | ✅ |
| 3 | صفحة تسجيل الدخول | ✅ |
| 4 | الـ Layout والـ Sidebar الجانبي | ✅ |
| 5 | لوحة القيادة (Home) | ✅ |
| 6 | نموذج العضو + صفحة التفاصيل | ✅ |
| 7 | قائمة الأعضاء + المرشّحات + Skeleton | ✅ |
| 8 | حالات Loading/Empty/Error + Responsive + Print | ✅ |
| 9 | الوصوليّة (A11y) النهائيّة | ✅ |

**المُنجَزات الكليّة:**
- `client/src/index.css` — نظام تصميم كامل بـ tokens دلاليّة.
- `client/src/components/Layout.tsx` — شريط جانبي قابل للطيّ + skip link + tooltips.
- `client/src/components/BrandMark.tsx` — هويّة بصريّة موحَّدة.
- `client/src/pages/Login.tsx` — صفحة دخول جذّابة بـ split layout.
- `client/src/pages/Home.tsx` — لوحة قيادة بإحصاءات ماليّة + مخطّط.
- `client/src/pages/Members.tsx` — جدول CRM-style بـ ٣ مرشّحات + Skeleton + Empty states ذكيّة.
- `client/src/pages/AddMember.tsx` — نموذج بأقسام دلاليّة + sticky save bar.
- `client/src/pages/MemberDetails.tsx` — Hero + بطاقات إحصاء + AlertDialog + Textarea.
- `docs/UI_UX_IMPROVEMENT_PLAN.md` — توثيق كامل لكلّ مرحلة بنتائج اختبار وقياس تأثير.
